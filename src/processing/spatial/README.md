# Spatial Processing

This category contains nodes that handle spatial layout, channel placement, or
microphone-array processing.

Examples:

- pan
- stereo widening
- downmix
- upmix
- beamforming
- source separation
- binaural rendering
- ambisonics processing

Single-device adapters may expose the channels delivered by a device, but
channel layout changes belong in explicit spatial processing nodes.
