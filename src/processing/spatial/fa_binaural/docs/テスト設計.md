# fa_binaural テスト設計

## Unit Test

- `FA-BINAURAL-SPEC-001`: package 化前でも README、仕様書、アルゴリズム詳細説明書、テスト設計、backend docs が存在する。
- `FA-BINAURAL-SPEC-002`: `package.xml` と `CMakeLists.txt` を package 化前に置かない。
- `FA-BINAURAL-SPEC-003`: 仕様書が missing HRTF profile fallback を禁止する。
- `FA-BINAURAL-SPEC-004`: backend docs が ROS2 topic/message dependency を禁止する。

## Integration Test

ROS2 package 化後に、known HRTF fixture で 2ch output contract と metadata 保持を確認する。

## Launch Test

ROS2 package 化後に、missing params file、missing HRTF profile、input layout mismatch で起動失敗することを確認する。

