# fa_pan テスト設計

## traceability

| test ID | 対応仕様 | 観点 |
| --- | --- | --- |
| `FA-PAN-TC-001` -> `FA-PAN-SPEC-001` | pan boundary | constant-power pan を行う spatial processing node であること。 |
| `FA-PAN-TC-002` -> `FA-PAN-SPEC-002` | non-goals | device I/O、format conversion、filter、denoise、analysis metadata を持たないこと。 |
| `FA-PAN-TC-003` -> `FA-PAN-SPEC-003` | topic / stream identity | topic と stream ID を分離し、衝突を fail closed すること。 |
| `FA-PAN-TC-004` -> `FA-PAN-SPEC-004` | required startup config | pan position、expected stereo format、QoS、diagnostics parameter を fail closed で検証すること。 |
| `FA-PAN-TC-005` -> `FA-PAN-SPEC-005` | runtime frame validation | source / stream identity、stereo format、data alignment、sample range を検証すること。 |
| `FA-PAN-TC-006` -> `FA-PAN-SPEC-006` | output metadata | output が `stream_id` と `data` だけを更新すること。 |
| `FA-PAN-TC-007` -> `FA-PAN-SPEC-007` | backend boundary | sample loop、decode / encode、constant-power gain を ROS-free backend に閉じること。 |
| `FA-PAN-TC-008` -> `FA-PAN-SPEC-008` | no hidden correction | invalid sample を typed status で返し、clamp / normalize で補正しないこと。 |
| `FA-PAN-TC-009` -> `FA-PAN-SPEC-009` | diagnostics | pan position、左右 gain、counter が diagnostics に含まれること。 |
| `FA-PAN-TC-010` -> `FA-PAN-SPEC-010` | launch contract | launch file が `node_name` / `config_file` だけを要求し、package default を推測しないこと。 |
| `FA-PAN-TC-011` -> `FA-PAN-SPEC-011` | colcon coverage | pytest / backend gTest と lint dependency が colcon manifest 上で成立すること。 |

## Unit Contract Tests

`test/unit/test_fa_pan_audio_frame_contract.py` は package contract を静的に検査する。

- default config が stereo FLOAT32LE interleaved contract を持つこと
- node が unrelated responsibilities を持たないこと
- invalid config で fail closed する validation があること
- runtime frame contract を処理前に検証すること
- ROS topic と `AudioFrame.stream_id` を分離し、`input_stream_id` / `output.stream_id` を使うこと
- `stream_id` を `output.stream_id` に更新し、それ以外の metadata を不要に書き換えないこと
- node が sample loop / FLOAT32LE decode / encode を直接持たず、ROS-free backend へ委譲すること
- backend に constant-power gain の式が実装されていること
- backend が finite normalized `[-1, 1]` 違反を typed status で返し、node が frame を drop すること
- diagnostics が `pan.position`、左右 gain、counter を含むこと
- standard processing package layout を満たすこと
- `ament_cmake_pytest` / `ament_cmake_gtest` で colcon から Python contract test と C++ backend test が登録されること

## Backend C++ Tests

`test/cpp/test_internal_constant_power_pan_backend.cpp` は `internal_constant_power_pan` を直接検査する。

- invalid `pan.position` で constructor が失敗すること
- `-1.0` / `0.0` / `1.0` の constant-power gain が期待値になること
- stereo FLOAT32LE byte列を pan 後の byte列に変換すること
- empty / misaligned / non-finite / range外入力を typed status で拒否すること
- 失敗時に既存 output buffer を書き換えないこと
- 未定義 status を正常な unknown 文字列へ丸めず、invariant error として扱うこと

## Integration Tests

将来の integration test では、実 ROS2 graph 上で `fa_pan/input` から `fa_pan/output` への publish を確認する。入力 frame の `stream_id` が `input_stream_id` と一致すること、出力 frame の `stream_id` が `output.stream_id` に更新されること、`epoch` と source identity が保持されること、invalid frame が publish されないことを確認対象にする。

## Launch Tests

将来の launch test では `launch/fa_pan.launch.py` が default config を読み込み、`fa_pan_node` を起動できることを確認する。
