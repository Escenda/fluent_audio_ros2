# fa_kws アルゴリズム詳細説明書

1. `AudioFrame` を mono float samples に変換する。
2. 最新の `VadState.probability` を読む。
3. backend `process()` に samples, sample rate, VAD probability, timestamp を渡す。
4. detection があれば `WakeWordResult` を publish する。

VAD probability は gating signal です。VAD 推定は `fa_vad` に分離します。
