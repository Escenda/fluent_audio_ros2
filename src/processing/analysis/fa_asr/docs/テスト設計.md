# fa_asr テスト設計

## Unit Test

- `FA-ASR-SPEC-001`: backend.name validation。
- `FA-ASR-SPEC-002`: unknown backend validation。
- `FA-ASR-SPEC-003`: backend ごとの required parameter と class 分離。
- `FA-ASR-SPEC-004`: AudioFrame decode validation。
- `FA-ASR-SPEC-005`: VAD end finalize。
- `FA-ASR-SPEC-006`: backend timeout/error mapping。
- `FA-ASR-SPEC-007`: default config の `backend.args` は空で、backend ごとに明示しないと起動失敗。

## Integration Test

- fixture PCM を local command mock に渡し、transcript が publish されることを確認する。

## Launch Test

- default config の `backend.name` が空で、明示設定なしでは起動失敗することを確認する。
- unknown backend で起動失敗することを確認する。

## Fixtures

- short utterance PCM16LE
- silence PCM16LE
- backend error fixture
