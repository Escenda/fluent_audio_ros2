# fa_interleave テスト設計

## Contract test

`test/unit/test_fa_interleave_audio_frame_contract.py` は、config validation、runtime frame rejection、metadata update、unsupported capability rejection を検証する。
`test/cpp/test_internal_layout_reorder_backend.cpp` は ROS-free backend contract を実 byte列で検査する。

## 検証項目

- default config が 16000Hz / FLOAT32LE / 32bit / 2ch / `interleaved` -> `planar` を明示する
- default config が ROS topic identity と `AudioFrame.stream_id` を分離していること
- device I/O、resampling、sample format conversion、bit-depth conversion、channel count conversion、gain、limiter、filtering の責務を持たない
- 起動時に未対応 layout / format combination を reject する
- 起動時に空 stream identity、raw/resolved topic collision、input/output stream identity collision を reject する
- runtime frame が `source_id`、`input_stream_id`、format、layout、channel、data size を満たさない場合 warning + drop する
- `stream_id` を `output.stream_id` に、`layout` を `output.layout` に更新し、他 metadata を保持する
- backend API test が interleaved -> planar と planar -> interleaved の byte-preserving reorder を検証する
- backend が misaligned payload を `FrameContractStatus` で拒否し、出力 buffer を更新しない
- unsupported encoding / bit depth / sample rate / channels / layout は numeric conversion せず drop または起動失敗にする

## 実行コマンド

```bash
env PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -p no:cacheprovider src/processing/format/fa_interleave/test/unit -q
```

## 仕様とテストケース対応

| TC ID | 対応 SPEC | 自動テスト | 種別 |
| --- | --- | --- | --- |
| `FA-INTERLEAVE-TC-001` -> `FA-INTERLEAVE-SPEC-001` | `FA-INTERLEAVE-SPEC-001` | behavior checks verify only layout reorder, with no I/O, codec, resample, sample-format, bit-depth, channel-count, gain, limiter, filter, or noise processing | behavior boundary |
| `FA-INTERLEAVE-TC-002` -> `FA-INTERLEAVE-SPEC-002` | `FA-INTERLEAVE-SPEC-002` | config validation requires explicit sample rate, channels, encoding, bit depth, input/output layout, and stream identity split | config validation |
| `FA-INTERLEAVE-TC-003` -> `FA-INTERLEAVE-SPEC-003` | `FA-INTERLEAVE-SPEC-003` | startup rejects unsupported layout conversion and stream/topic identity collisions | launch/config fail-closed |
| `FA-INTERLEAVE-TC-004` -> `FA-INTERLEAVE-SPEC-004` | `FA-INTERLEAVE-SPEC-004` | backend API accepts only explicit `interleaved -> planar` or `planar -> interleaved` contracts | backend API |
| `FA-INTERLEAVE-TC-005` -> `FA-INTERLEAVE-SPEC-005` | `FA-INTERLEAVE-SPEC-005` | runtime validation and backend API reject unsupported metadata and misaligned payload before mutation | frame rejection |
| `FA-INTERLEAVE-TC-006` -> `FA-INTERLEAVE-SPEC-006` | `FA-INTERLEAVE-SPEC-006` | behavior checks preserve metadata and update only `stream_id`, `layout`, and byte-reordered `data` | frame behavior |
| `FA-INTERLEAVE-TC-007` -> `FA-INTERLEAVE-SPEC-007` | `FA-INTERLEAVE-SPEC-007` | backend API verifies byte-preserving reorder for both directions and no numeric sample conversion | backend runtime |
| `FA-INTERLEAVE-TC-008` -> `FA-INTERLEAVE-SPEC-008` | `FA-INTERLEAVE-SPEC-008` | `test/cpp/test_internal_layout_reorder_backend.cpp::RejectsMisalignedPayloadWithoutMutatingOutput`, `test/cpp/test_internal_layout_reorder_backend.cpp::RejectsRuntimeMetadataMismatch` | backend runtime |
| `FA-INTERLEAVE-TC-009` -> `FA-INTERLEAVE-SPEC-009` | `FA-INTERLEAVE-SPEC-009` | `test/cpp/test_internal_layout_reorder_backend.cpp::RejectsUnhandledStatusValues` | backend runtime |
| `FA-INTERLEAVE-TC-010` -> `FA-INTERLEAVE-SPEC-010` | `FA-INTERLEAVE-SPEC-010` | executable tests cover supported layout conversion, unsupported capability rejection, malformed frame rejection, and no output mutation on failure | executable coverage |
