# fa_resample 仕様書

## 目的

`fa_resample` は `AudioFrame.sample_rate` だけを変更する format processing node である。
入力音声の sample format、bit depth、channel count、interleave 形式、gain、noise、発話判定、文字起こしは変更しない。

現行 capability は `FLOAT32LE` / 32-bit / `interleaved` 入力を、同じ sample format / bit depth / channel count / layout のまま `target_sample_rate` へ変換することである。

`PCM16LE` / `PCM32LE` から `FLOAT32LE` への変換は `fa_sample_format` の責務である。channel 変換は channel conversion node の責務である。`fa_resample` は hidden conversion、clamp、normalize、denoise、VAD、ASR を持たない。

## 責務分離

ROS node は次を担当する。

- parameter validation
- mic/ref topic subscription と publication
- `source_id` / `stream_id` validation
- output `AudioFrame` mapping
- drop counter と warning log
- diagnostics publish

resampler backend は次を担当する。

- `FLOAT32LE` / 32-bit / `interleaved` frame contract validation
- finite normalized sample validation
- stream ごとの resampler state
- sample rate conversion
- backend metrics
- typed `ProcessStatus` / `FrameContractStatus`

backend は ROS2 topic、publisher/subscriber、`fa_interfaces/msg/AudioFrame` を知らない。

## 入力 topic

| parameter | default | 内容 |
| --- | --- | --- |
| `mic.input_topic` | `audio/frame` | microphone / source 側 frame |
| `ref.input_topic` | `audio/output/frame` | AEC reference 用 frame |

## 出力 topic

| parameter | default | 内容 |
| --- | --- | --- |
| `mic.output_topic` | `audio/resample16k/mic` | resampled mic stream |
| `ref.output_topic` | `audio/resample16k/ref` | resampled reference stream |

## stream identity

| parameter | default | 内容 |
| --- | --- | --- |
| `mic.input_stream_id` | `audio/float32/mic` | mic 入力として受け入れる `AudioFrame.stream_id` |
| `mic.output.stream_id` | `audio/preprocessed/mono16k` | resampled mic 出力の `AudioFrame.stream_id` |
| `ref.input_stream_id` | `audio/playback/main` | reference 入力として受け入れる `AudioFrame.stream_id` |
| `ref.output.stream_id` | `audio/preprocessed/ref16k` | resampled reference 出力の `AudioFrame.stream_id` |

`*.input_topic` / `*.output_topic` は ROS transport topic、`*.input_stream_id` / `*.output.stream_id` は frame identity である。
enabled stream では topic 名と stream ID が同一視できる設定、または input/output stream ID が同一の設定を起動失敗にする。

## parameters

| parameter | 意味 |
| --- | --- |
| `target_sample_rate` | 出力 sample rate。`> 0` が必須 |
| `backend.name` | 必須。`internal_linear_resampler`、`speexdsp`、`soxr` のいずれか |
| `backend.quality` | backend ごとの品質指定。下記参照 |
| `input.encoding` | `FLOAT32LE` 固定。別値は起動失敗 |
| `input.bit_depth` | `32` 固定。別値は起動失敗 |
| `input.layout` | `interleaved` 固定。別値は起動失敗 |
| `output.encoding` | `FLOAT32LE` 固定。別値は起動失敗 |
| `output.bit_depth` | `32` 固定。別値は起動失敗 |
| `mic.enabled` | mic stream の有効化 |
| `mic.input_stream_id` | mic 入力 stream identity |
| `mic.output.stream_id` | mic 出力 stream identity |
| `ref.enabled` | reference stream の有効化 |
| `ref.input_stream_id` | reference 入力 stream identity |
| `ref.output.stream_id` | reference 出力 stream identity |
| `qos.depth` | audio frame QoS depth。`> 0` が必須 |
| `qos.reliable` | reliable / best effort |
| `diagnostics.publish_period_ms` | diagnostics publish 周期。`> 0` が必須 |
| `diagnostics.qos.depth` | diagnostics QoS depth。`> 0` が必須 |
| `diagnostics.qos.reliable` | diagnostics QoS reliability |

## backend selection

`backend.name` は明示必須である。未知の backend 名は起動失敗にする。

| `backend.name` | `backend.quality` | 用途 |
| --- | --- | --- |
| `internal_linear_resampler` | 未設定または `debug_reference` | debug / reference 用の内部線形 backend |
| `speexdsp` | integer `0..10` | realtime voice pipeline 候補 |
| `soxr` | `QQ` / `LQ` / `MQ` / `HQ` / `VHQ` | high-quality resampling / 評価 / recording 用 |

`speexdsp` 選択時に `libspeexdsp.so.1` が見つからない場合、起動失敗にする。
`soxr` 選択時に `libsoxr.so.0` が見つからない場合、起動失敗にする。
選択した backend の runtime dependency が無い場合に `internal_linear_resampler` へ自動 fallback してはならない。

`package.xml` は runtime library の ROS package contract として `libspeexdsp1` と `libsoxr0` を
`exec_depend` に宣言する。C++ 実装は SpeexDSP / SoXR の header を直接 include せず、
`libspeexdsp.so.1` / `libsoxr.so.0` を `dlopen` するため、これらを build dependency や dev package
dependency として扱わない。

## VLAbor overlay image contract

VLAbor base image `ghcr.io/takatronix/vlabor-local:latest` 自体に `fa_resample` 用 runtime dependency を
直接追加したとは扱わない。`ros2_ws/src/vlabor_ros2/docker/Dockerfile.local` が `libsoxr0` /
`libspeexdsp1` を持つ、または heavy VLAbor base image の full rebuild が `fa_resample` completion proof で
ある、とは書かない。

親 repo には FluentAudio 用の薄い overlay image contract がある。

| path | contract |
| --- | --- |
| `docker/vlabor/Dockerfile.fluent-audio` | `ghcr.io/takatronix/vlabor-local:latest` を base にし、`libsoxr0` / `libspeexdsp1` だけを追加する |
| `docker/vlabor/build-fluent-audio-image` | overlay image `vlabor-fluent-audio:local` を build する |

この overlay は `fa_resample` の runtime library を VLAbor 実行環境へ足すための親 repo 側の未コミット編集である。
release / publish に採用するかは、別途運用判断と commit / publish 手順が必要である。

## input / output capability

入力として受け付ける `AudioFrame` contract は次に限定する。

| field | supported value |
| --- | --- |
| `source_id` | 空文字不可 |
| `stream_id` | 対応する `*.input_stream_id` と完全一致 |
| `encoding` | `FLOAT32LE` |
| `bit_depth` | `32` |
| `layout` | `interleaved` |
| `channels` | `>= 1` |
| `sample_rate` | `> 0` |
| `data` | non-empty、`channels * sizeof(float)` に整列 |
| sample value | finite、normalized range `[-1.0, 1.0]` |

出力 `AudioFrame` は次の contract を持つ。

| field | 出力値 |
| --- | --- |
| `source_id` | 入力から維持 |
| `stream_id` | 対応する `*.output.stream_id` |
| `encoding` | `FLOAT32LE` |
| `sample_rate` | `target_sample_rate` |
| `channels` | 入力と同一 |
| `bit_depth` | `32` |
| `layout` | `interleaved` |
| `epoch` | 入力から維持 |
| `header` | 入力から維持 |

capability 外の frame は backend state を変える前に rejection する。非対応の sample format、bit depth、channel count、layout、値域を補正して成功扱いしない。

## stream state

backend は stream ごとに state を保持する。state の map key は `stream_id` であり、作成時の stream contract は次を含む。

- `stream_id`
- frame contract: `encoding`、`sample_rate`、`channels`、`bit_depth`、`layout`
- `target_sample_rate`
- backend name
- backend quality

同一 `stream_id` の途中で frame contract、`target_sample_rate`、backend name、backend quality が変わった場合は stream contract violation とする。
backend を chunk ごとに hidden reinitialization してはならない。fractional phase、filter history、pending output、library state は stream state として保持する。

## lifecycle

起動時に parameter を検証し、選択 backend を生成し、enabled stream の publisher / subscriber と diagnostics publisher を作る。

起動失敗条件:

- required parameter が未設定
- `backend.name` が未知
- `backend.quality` が backend の許可範囲外
- 選択 backend の runtime dependency が無い
- `target_sample_rate <= 0`
- `input.encoding != FLOAT32LE`
- `input.bit_depth != 32`
- `input.layout != interleaved`
- `output.encoding != FLOAT32LE`
- `output.bit_depth != 32`
- QoS / diagnostics depth または diagnostics period が `<= 0`
- enabled stream の topic / stream ID が空
- topic と stream ID が同一視できる
- input/output stream ID が同一

## error handling

不正 frame は drop し、drop counter を増やす。backend は `ProcessStatus` と `FrameContractStatus` を返す。invalid input を空 vector だけで表現しない。

drop 条件:

- `source_id` / `stream_id` が空
- `stream_id` が対応する `mic.input_stream_id` / `ref.input_stream_id` と一致しない
- `sample_rate == 0` または `channels == 0`
- `encoding != FLOAT32LE`
- `bit_depth != 32`
- `layout != interleaved`
- `data` が空、または `channels * sizeof(float)` の倍数ではない
- sample が finite ではない、または normalized range `[-1.0, 1.0]` を外れる
- 同一 `stream_id` の stream contract が途中で変わる
- backend process / encode に失敗する

## diagnostics metrics

diagnostics には少なくとも次を出す。

| metric | 内容 |
| --- | --- |
| `algorithmic_delay_input_samples` | 入力 sample rate 基準の algorithmic delay |
| `algorithmic_delay_output_samples` | 出力 sample rate 基準の algorithmic delay |
| `algorithmic_delay_ms` | 出力 sample rate 基準の ms 換算 delay |
| `processing_time_mean_ms` | backend process call の平均処理時間 |
| `processing_time_max_ms` | backend process call の最大処理時間 |
| `input_frames_total` | backend が受け取った累積 input frame 数 |
| `output_frames_total` | backend が出した累積 output frame 数 |
| `expected_output_frames` | input/output sample rate 比から計算した期待累積 output frame 数 |
| `frame_count_error_samples` | `output_frames_total - expected_output_frames` の丸め差 |

## quality test XML metrics

`test/cpp/test_resampler_quality_metrics.cpp` は、backend を実際に処理して得た結果を GTest XML property として残す。

音質差の test-only metrics は production diagnostics ではない。対象は次である。

- `<prefix>_rms_error`
- `<prefix>_peak_error`
- `<prefix>_snr_db`
- `<prefix>_compared_samples`
- `reference_impulse_peak_offset_samples`

backend 別の delay / processing / frame count metrics は `BackendMetrics` 由来であり、音質差そのものではない。
品質比較テストでは、同じ backend prefix で次を GTest XML に記録する。

- `<prefix>_algorithmic_delay_input_samples`
- `<prefix>_algorithmic_delay_output_samples`
- `<prefix>_algorithmic_delay_ms`
- `<prefix>_processing_time_mean_ms`
- `<prefix>_processing_time_max_ms`
- `<prefix>_input_frames_total`
- `<prefix>_output_frames_total`
- `<prefix>_frame_count_error_samples`

## metrics reporter

`lib/fa_resample/fa_resample_metrics_report.py` は stdlib-only の metrics reporter である。
`fa_resample_backend_test.gtest.xml` 単体、または `fa_resample` GTest XML を含む directory を読み、quality metrics と backend metrics を人間が読める形に整形する。

reporter が扱う quality metrics は次である。

- `rms_error`
- `peak_error`
- `snr_db`
- `compared_samples`

reporter が扱う backend metrics は次である。

- `algorithmic_delay_input_samples`
- `algorithmic_delay_output_samples`
- `algorithmic_delay_ms`
- `processing_time_mean_ms`
- `processing_time_max_ms`
- `input_frames_total`
- `output_frames_total`
- `frame_count_error_samples`

reporter は incomplete metric group を成功扱いしない。欠けた metric group は失敗として扱い、欠損した品質・backend 指標を隠さない。

## metrics report の読み方

metrics report は、test signal に対する各 backend の出力を SoXR `VHQ` reference と比較した報告である。
これは波形が選んだ reference にどれだけ近いかを示す test-only evidence であり、人間の聴感 MOS や ASR 精度を
直接測った証拠ではない。

品質差を見るときは、`snr_db`、`rms_error`、`peak_error` を同じ metric group の中で読む。

| metric | 読み方 |
| --- | --- |
| `snr_db` | reference 信号の power に対して error power がどれだけ小さいかを dB で表す。高いほど reference に近い |
| `rms_error` | 比較区間全体の平均的な波形誤差。小さいほど reference に近い |
| `peak_error` | 比較区間内で最大の瞬間誤差。小さいほど最大ずれが小さい |
| `algorithmic_delay_ms` | backend が resampling のために持つ algorithmic delay。小さいほど realtime pipeline に載せやすい |

SNR は dB 表記なので、差分は線形ではない。SNR が +20 dB 高い場合、error power はおおよそ 100 倍小さく、
RMS error はおおよそ 10 倍小さい。SNR が +26 dB 高い場合、error power はおおよそ 400 倍小さく、
RMS error はおおよそ 20 倍小さい。

現在の verified examples は次である。

| metric group | `snr_db` | `rms_error` | `peak_error` | `algorithmic_delay_ms` |
| --- | --- | --- | --- | --- |
| SoXR HQ / `passband_soxr_hq` | `1.357997764820949e+02` | `3.394284426758690e-08` | `1.490116119384766e-07` | `2.131250000000000e+01` |
| SpeexDSP q6 / `speex_q6_passband` | `1.094336419058643e+02` | `5.941855108704546e-07` | `1.370906829833984e-06` | `3.000000000000000e+00` |

この test では SoXR HQ と SpeexDSP q6 の SNR 差は約 26.36 dB である。これは SoXR HQ が、この test signal
と SoXR VHQ reference に対して、SpeexDSP q6 よりかなり近い波形を出したことを意味する。一方で SpeexDSP q6 は
`algorithmic_delay_ms` がかなり小さいため、追加される波形誤差が用途上許容できる場合の low-latency realtime voice
pipeline 候補になる。

backend 選定では次のように読む。

- SoXR HQ は quality-first の recording / evaluation / offline 寄り、または latency-tolerant な経路の候補である。
- SpeexDSP q6 は low-latency realtime voice pipeline の候補である。ただし waveform fidelity は SoXR VHQ reference
  から見て SoXR HQ より離れる。
- SoXR MQ は internal linear より高品質で、HQ より delay を抑えたい場合の balanced candidate である。

これらの判断は、検証済み test signal における SoXR VHQ reference への波形近さと backend delay の比較である。
SoXR HQ があらゆる入力、聴感、ASR、後段 AI に対して常に客観的に優れる、とは扱わない。

## non-goals

- device input
- codec decode / encode
- sample format conversion
- bit depth conversion
- channel conversion / remix / downmix
- interleaved / planar conversion
- gain / normalize / limiter / clamp
- denoise / AGC
- VAD / KWS / ASR / Turn Detector

## 実装・検証境界

実装済みとして扱う範囲:

- `internal_linear_resampler`、`speexdsp`、`soxr` の明示 backend selection
- unknown backend の startup failure
- backend quality validation
- `FLOAT32LE` / 32-bit / interleaved capability validation
- stream contract violation の rejection
- diagnostics metrics publish
- `libspeexdsp1` / `libsoxr0` の runtime dependency package contract
- quality comparison test XML への音質差 metrics と backend metrics の記録
- stdlib-only metrics reporter の install と incomplete metric group failure
- `test/cpp/test_resample_graph.cpp` による SoXR / SpeexDSP backend 選択状態の ROS graph smoke

検証済みとして受け取っている範囲:

- `docker/vlabor/build-fluent-audio-image` が約 12 秒で成功し、base digest `sha256:b709...` から
  `vlabor-fluent-audio:local` を生成した
- overlay image build の apt 出力で `libsoxr0 is already the newest version` と
  `libspeexdsp1` の新規 install を確認した
- `docker run --rm --entrypoint bash vlabor-fluent-audio:local -lc 'dpkg-query -W libsoxr0 libspeexdsp1 && ls -l /lib/x86_64-linux-gnu/libsoxr.so.0* /lib/x86_64-linux-gnu/libspeexdsp.so.1*'`
  が成功し、`libsoxr0:amd64 0.1.3-4build2`、`libspeexdsp1:amd64 1.2~rc1.2-1.1ubuntu3`、
  および両方の `.so` symlink / file を確認した
- current running VLAbor container へ `docker compose -f docker/vlabor/compose.yml exec vlabor` で入り、
  `colcon build --packages-select fa_resample` が通過
- current running VLAbor container へ `docker compose -f docker/vlabor/compose.yml exec vlabor` で入り、
  `colcon test --packages-select fa_resample` が通過
- `colcon test-result --verbose --test-result-base build/fa_resample/test_results/fa_resample` 上、
  `fa_resample` は 51 tests、0 errors、0 failures、0 skipped
- SoXR graph smoke は `backend.name=soxr`、`backend.quality=MQ` で実際に `FaResampleNode` を起動し、
  `AudioFrame` を publish / subscribe して `/diagnostics` を確認する
- SpeexDSP graph smoke は `backend.name=speexdsp`、`backend.quality=6` で実際に `FaResampleNode` を起動し、
  `AudioFrame` を publish / subscribe して `/diagnostics` を確認する
- 各 graph smoke は `/diagnostics` の `backend.name`、`backend.quality`、`algorithmic_delay_ms`、
  `processing_time_mean_ms`、`input_frames_total`、`output_frames_total`、`expected_output_frames`、
  `frame_count_error_samples` を確認する
- GTest XML に quality metrics と backend 別 delay / processing / frame count metrics が実際に記録されること
- metrics reporter が GTest XML から quality / backend metrics を出力し、incomplete metric group を失敗にすること
- metrics report は `passband_soxr_mq` について `rms_error=1.134304080736542e-07`、
  `peak_error=2.793967723846436e-07`、`snr_db=1.253201512234613e+02`、
  `algorithmic_delay_ms=1.500000000000000e+01` を出力した
- metrics report は `passband_soxr_hq` について `rms_error=3.394284426758690e-08`、
  `peak_error=1.490116119384766e-07`、`snr_db=1.357997764820949e+02`、
  `algorithmic_delay_ms=2.131250000000000e+01` を出力した
- metrics report は `speex_q6_passband` について `rms_error=5.941855108704546e-07`、
  `peak_error=1.370906829833984e-06`、`snr_db=1.094336419058643e+02`、
  `algorithmic_delay_ms=3.000000000000000e+00` を出力した
- running VLAbor container と PowerConf S3 を使い、`fluent_audio_system` temp config による
  `fa_in -> fa_sample_format -> fa_resample` real-device smoke を SoXR / SpeexDSP の selected backend で確認済み
- SoXR/MQ real-device smoke は `/fa_real_soxr/audio/resample16k` に output frame を publish し、
  `stream_id=audio/real_soxr/preprocessed/mono16k`、`source_id=hw:CARD=S3,DEV=0`、
  `sample_rate=16000`、`encoding=FLOAT32LE` を確認した。diagnostics は
  `backend.name=soxr`、`backend.quality=MQ`、`algorithmic_delay_ms=22.500000`、
  `processing_time_mean_ms=0.056465`、`input_frames_total=72000`、
  `output_frames_total=23640`、`frame_count_error_samples=-360` を含む
- SpeexDSP/6 real-device smoke は `/fa_real_speexdsp/audio/resample16k` に output frame を publish し、
  `stream_id=audio/real_speexdsp/preprocessed/mono16k`、`source_id=hw:CARD=S3,DEV=0`、
  `sample_rate=16000`、`encoding=FLOAT32LE` を確認した。diagnostics は
  `backend.name=speexdsp`、`backend.quality=6`、`algorithmic_delay_ms=3.000000`、
  `processing_time_mean_ms=0.066197`、`input_frames_total=74880`、
  `output_frames_total=24960`、`frame_count_error_samples=0` を含む
- SoXR graph smoke は高振幅 ramp で `FLOAT32LE` encoding failed になった履歴がある。production contract は弱めず、
  graph smoke 用入力だけを低振幅 multi-tone にした

未検証として残す範囲:

- overlay image の publish / release 採用
- deployment / compose の正式な image 向き先変更
- compose / deployment を `vlabor-fluent-audio:local` に切り替えた状態での `fa_resample` package build/test
- 親 repo overlay files の commit
- release 状態として扱うための運用判断
- full workspace build/test
