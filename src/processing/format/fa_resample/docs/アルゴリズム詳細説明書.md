# fa_resample アルゴリズム詳細説明書

## 処理手順

```text
AudioFrame
  ↓
validate metadata and byte length
  ↓
decode PCM16LE or float32 to interleaved float32
  ↓
linear interpolation resample
  ↓
encode to configured output bit depth
  ↓
publish AudioFrame
```

## resampling

出力 frame 数は次で決める。

```text
out_frames = round(in_frames * target_sample_rate / input_sample_rate)
```

各出力 sample は入力位置 `src_pos = out_index * input_sample_rate / target_sample_rate` を使い、隣接 2 sample の線形補間で求める。

```text
y[i] = (1 - frac) * x[floor(src_pos)] + frac * x[min(floor(src_pos)+1, last)]
```

## frame / buffer 前提

- 入力は interleaved PCM。
- `bit_depth` は 16 or 32。
- `data.size()` は `channels * bytes_per_sample` の倍数。
- 出力 sample rate は 16kHz 固定。

## known limitations

- 現在の resampler は線形補間であり、band-limited resampling ではない。
- channel 数は変えない。channel conversion は `processing/format/fa_channel` の責務にする。
