# fa_format

Roadmap directory for the format wrapper node that composes explicit format
conversion stages without hiding resample, channel, bit-depth, codec, or sample
format changes inside `fa_in` or `fa_out`.

This is not a ROS 2 package yet. Do not add `package.xml` until the package has
its specification, algorithm notes, backend documentation, launch contract, and
tests.
