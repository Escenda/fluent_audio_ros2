# fa_bit_depth アルゴリズム詳細説明書

## 0. backend 境界

`internal_integer_bit_depth` backend は ROS2 topic/message を知らない。入力は byte列と `FrameContract`、出力は byte列と `ProcessResult` である。
node は `AudioFrame` から `FrameContract` を作り、backend の成功時だけ `AudioFrame.data` と format metadata を更新して publish する。
backend が失敗した場合、node は frame を drop し、古い出力を publish しない。

## 1. PCM16LE から PCM32LE

入力 byte列を little-endian の signed 16 bit PCM として 2 byte ずつ読む。
変換は normalized float を経由せず、16 bit PCM word を 32 bit PCM word の上位 16 bit に配置する。

```text
raw16 = uint16 little-endian sample
raw32 = raw16 << 16
```

例:

| PCM16LE sample | PCM32LE sample |
| --- | --- |
| `0x0000` | `0x00000000` |
| `0x7fff` | `0x7fff0000` |
| `0x8000` | `0x80000000` |
| `0xffff` | `0xffff0000` |

この変換は 16 bit sample の順位と符号を保持し、追加される下位 16 bit は 0 で埋める。

## 2. 非対応変換

lossy PCM32LE/32 -> PCM16LE/16 は扱わない。下位 bit の破棄、dither、rounding、clipping、normalized range conversion は、この node の責務ではない。必要な場合は明示的な policy を持つ別 node として追加する。

## 3. Byte count validation

`AudioFrame.data` は次を満たす必要がある。

```text
bytes_per_sample = input.bit_depth / 8
bytes_per_frame = expected.channels * bytes_per_sample
data.size > 0
data.size % bytes_per_frame == 0
```

不一致の場合、frame 境界が壊れているため publish せず drop する。

`FrameContract` は encoding、bit depth、sample rate、channels、layout、data size を含む。backend は config と runtime contract が一致しない場合、`FrameContractStatus` で理由を返す。

## 4. 変換しない項目

sample rate、channels、layout は変更しない。resample や channel 変換は別 package の責務である。
振幅加工も行わない。PCM integer bit depth の再配置だけを行い、gain、limiter、loudness normalize は行わない。
