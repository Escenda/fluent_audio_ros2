fa_latency_compensation:
  ros__parameters:
    input_topic: "audio/frame"
    output_topic: "audio/latency_compensated/frame"

    compensation:
      offset_ms: 0.0

    expected:
      sample_rate: 16000
      channels: 1
      encoding: "FLOAT32LE"
      bit_depth: 32
      layout: "interleaved"

    qos:
      depth: 10
      reliable: false

    diagnostics:
      publish_period_ms: 1000
