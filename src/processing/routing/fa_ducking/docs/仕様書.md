# fa_ducking 仕様書

## 1. 目的

`fa_ducking` は、program `AudioFrame` stream に対し、sidechain `AudioFrame` stream の RMS を制御信号とする ducking を行う。

対象は routing/dynamics boundary の sidechain ducking であり、sidechain の混合、resampling、format conversion、limiter、compressor、normalize、output device playback は責務外である。

## 1.1. 仕様 ID

| ID | 内容 |
| --- | --- |
| `FA-DUCKING-SPEC-001` | `fa_ducking` は program stream に sidechain RMS を制御信号とする ducking を適用する routing/dynamics boundary node である。 |
| `FA-DUCKING-SPEC-002` | program / sidechain / output topic と stream identity はすべて明示され、topic identity と stream identity の衝突を起動時に拒否する。 |
| `FA-DUCKING-SPEC-003` | program / sidechain frame は `FLOAT32LE` / 32 bit / interleaved、expected sample rate / channels、finite normalized sample を満たす必要がある。 |
| `FA-DUCKING-SPEC-004` | sidechain threshold / max age、ducking gain / attack / release、expected format、QoS、diagnostics parameter は required で、不正値は fail closed する。 |
| `FA-DUCKING-SPEC-005` | sidechain 未受信 / stale / threshold 未満は明示的な inactive state として target gain `1.0` を使い、壊れた frame は recent state を無効化する。 |
| `FA-DUCKING-SPEC-006` | output metadata は program frame を基準にし、`stream_id` は `output.stream_id` へ更新する。 |
| `FA-DUCKING-SPEC-007` | 入力 / 出力 sample の非 finite や normalized range 違反は drop し、clamp / limiter / normalize で補正しない。 |
| `FA-DUCKING-SPEC-008` | RMS 計算、sidechain state、attack/release smoothing、program sample loop は ROS-free backend に閉じる。 |
| `FA-DUCKING-SPEC-009` | diagnostics は routing 設定、ducking state、sidechain state、counter を publish する。 |
| `FA-DUCKING-SPEC-010` | launch file は `node_name` / `config_file` を明示引数とし、package-local default config を推測しない。 |
| `FA-DUCKING-SPEC-011` | pytest / backend gTest contract は `colcon test` から実行される。 |

## 2. Package / Executable / Node

| 項目 | 値 |
| --- | --- |
| package | `fa_ducking` |
| executable | `fa_ducking_node` |
| node name | launch argument `node_name` で必ず明示。example config では `fa_ducking` |

## 3. 入出力

| 種別 | パラメータ | Message |
| --- | --- | --- |
| subscribe | `program_topic` | `fa_interfaces/msg/AudioFrame` |
| subscribe | `sidechain_topic` | `fa_interfaces/msg/AudioFrame` |
| publish | `output_topic` | `fa_interfaces/msg/AudioFrame` |

sidechain frame は RMS state の更新にのみ使う。sidechain sample を output data へ加算しない。

## 4. AudioFrame 契約

program / sidechain の入力 frame は以下を満たす必要がある。

| Field | 条件 |
| --- | --- |
| `source_id` | 非空 |
| `stream_id` | 非空。program は `program_stream_id`、sidechain は `sidechain_stream_id` と一致 |
| `sample_rate` | `expected.sample_rate` と一致、かつ `> 0` |
| `channels` | `expected.channels` と一致、かつ `> 0` |
| `encoding` | `FLOAT32LE` |
| `bit_depth` | `32` |
| `layout` | `interleaved` |
| `data` | 非空、かつ `channels * sizeof(float)` の倍数 |

入力 sample は finite かつ `[-1.0, 1.0]` の正規化範囲内でなければならない。出力 sample が finite でない、または正規化範囲を超える場合、その program frame は drop する。clamp や limiter による補正は行わない。

## 5. Parameter

すべて required であり、コード上に有効な runtime default はない。package launch の `config_file` も明示必須である。

| Parameter | 設定例 | 条件 |
| --- | ---: | --- |
| `program_topic` | `fa_ducking/program` | 非空、他 topic と distinct |
| `sidechain_topic` | `fa_ducking/sidechain` | 非空、他 topic と distinct |
| `output_topic` | `fa_ducking/output` | 非空、他 topic と distinct |
| `program_stream_id` | `audio/program/frame` | 非空、topic 名と一致不可 |
| `sidechain_stream_id` | `audio/sidechain/frame` | 非空、topic 名および他 stream と一致不可 |
| `output.stream_id` | `audio/ducked/frame` | 非空、topic 名および入力 stream と一致不可 |
| `sidechain.threshold_rms` | `0.05` | finite、`> 0.0`、`<= 1.0` |
| `sidechain.max_age_ms` | `100` | `> 0` |
| `ducking.gain_db` | `-12.0` | finite、`>= -96.0`、`< 0.0` |
| `ducking.attack_ms` | `10.0` | finite、`> 0.0` |
| `ducking.release_ms` | `250.0` | finite、`> 0.0` |
| `expected.sample_rate` | `16000` | `> 0` |
| `expected.channels` | `1` | `> 0` |
| `expected.encoding` | `FLOAT32LE` | 固定 |
| `expected.bit_depth` | `32` | 固定 |
| `expected.layout` | `interleaved` | 固定 |
| `qos.depth` | `10` | `> 0` |
| `qos.reliable` | `false` | bool |
| `diagnostics.qos.depth` | `10` | `> 0` |
| `diagnostics.qos.reliable` | `false` | bool |
| `diagnostics.publish_period_ms` | `1000` | `> 0` |

## 6. Sidechain state

valid sidechain frame を受信すると、frame RMS と受信時刻を recent sidechain state として保持する。program frame 処理時に、受信時刻からの経過が `sidechain.max_age_ms` 以内で、かつ RMS が `sidechain.threshold_rms` 以上の場合だけ active sidechain とする。

sidechain が未受信、stale、または threshold 未満の場合、target gain は `1.0` とする。これは sidechain inactive state の明示仕様であり、壊れた frame を正常扱いする fallback ではない。sidechain frame が契約違反または不正 sample を含む場合、その frame を drop し、recent sidechain state を無効化する。

## 7. Ducking state

起動直後の gain は `1.0` とする。active sidechain では `ducking.gain_db` を linear gain に変換した値を target gain とする。inactive sidechain では `1.0` を target gain とする。

target gain が current gain より小さい場合は `ducking.attack_ms`、大きい場合は `ducking.release_ms` から frame duration に基づく smoothing coefficient を計算する。program frame へは smoothed current gain を乗算する。

## 8. 出力 metadata

出力 frame は入力 program frame を基準に以下を保持・更新する。

| Field | 扱い |
| --- | --- |
| `header` | program 入力を保持 |
| `source_id` | program 入力を保持 |
| `sample_rate` | program 入力を保持 |
| `channels` | program 入力を保持 |
| `bit_depth` | program 入力を保持 |
| `encoding` | program 入力を保持 |
| `layout` | program 入力を保持 |
| `epoch` | program 入力を保持 |
| `stream_id` | `output.stream_id` に更新 |
| `data` | ducking 後 FLOAT32LE sample bytes |

## 9. Diagnostics

`diagnostics` topic に `diagnostic_msgs/msg/DiagnosticArray` を publish する。status values には少なくとも以下を含める。

- `program_topic`
- `sidechain_topic`
- `output_topic`
- `program_stream_id`
- `sidechain_stream_id`
- `output_stream_id`
- `sidechain_threshold_rms`
- `sidechain_max_age_ms`
- `ducking_gain_db`
- `ducking_gain_linear`
- `attack_ms`
- `release_ms`
- `current_gain`
- `last_target_gain`
- `last_sidechain_rms`
- `last_sidechain_age_ms`
- `last_sidechain_active`
- `program_frames_in`
- `program_frames_out`
- `program_frames_dropped`
- `sidechain_frames_in`
- `sidechain_frames_valid`
- `sidechain_frames_dropped`
- `sidechain_state_invalidations`
- `ducked_program_frames`
- `released_program_frames`
- `stale_sidechain_checks`

## 10. backend 境界

RMS 計算、recent sidechain state、attack/release smoothing、program sample loop は ROS-free backend `internal_sidechain_ducking` に閉じる。backend は ROS2 topic、`fa_interfaces/msg/AudioFrame`、diagnostics、publisher/subscriber を知らない。ROS node は topic subscription/publication、AudioFrame metadata validation、diagnostics、`stream_id` 更新を担当する。

## 11. 失敗時動作

- 起動時設定不正: 例外により node 起動を失敗させる。
- program frame 契約違反: warning を出して frame を drop する。
- sidechain frame 契約違反: warning を出して frame を drop し、recent sidechain state を無効化する。
- 入力 sample 不正: warning を出して対象 frame を drop する。
- 出力 sample 範囲超過: warning を出して program frame を drop する。この場合、candidate gain は current gain として確定しない。

意味を変える fallback、legacy alias、暗黙の補正は行わない。
