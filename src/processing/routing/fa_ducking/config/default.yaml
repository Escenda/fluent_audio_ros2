fa_ducking:
  ros__parameters:
    program_topic: "audio/program/frame"
    sidechain_topic: "audio/sidechain/frame"
    output_topic: "audio/ducked/frame"

    sidechain:
      threshold_rms: 0.05
      max_age_ms: 100

    ducking:
      gain_db: -12.0
      attack_ms: 10.0
      release_ms: 250.0

    expected:
      sample_rate: 16000
      channels: 1
      encoding: "FLOAT32LE"
      bit_depth: 32
      layout: "interleaved"

    qos:
      depth: 10
      reliable: false

    diagnostics:
      publish_period_ms: 1000
