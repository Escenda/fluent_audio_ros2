# fa_monitor_mix テスト設計

## traceability

| test ID | 対応仕様 | 観点 |
| --- | --- | --- |
| `FA-MONITOR-MIX-TC-001` -> `FA-MONITOR-MIX-SPEC-001` | monitor mix boundary | monitor bus 用の独立 mix stream を生成する routing node であること。 |
| `FA-MONITOR-MIX-TC-002` -> `FA-MONITOR-MIX-SPEC-002` | backend boundary | sample validation と mix を ROS-free backend に閉じること。 |
| `FA-MONITOR-MIX-TC-003` -> `FA-MONITOR-MIX-SPEC-003` | topic / stream identity | topic と stream ID を分離し、衝突を fail closed すること。 |
| `FA-MONITOR-MIX-TC-004` -> `FA-MONITOR-MIX-SPEC-004` | required startup config | input set、output identity、expected format、QoS、diagnostics parameter を required とすること。 |
| `FA-MONITOR-MIX-TC-005` -> `FA-MONITOR-MIX-SPEC-005` | FLOAT32 frame contract | source / stream identity、FLOAT32 format、data alignment、sample range を検証すること。 |
| `FA-MONITOR-MIX-TC-006` -> `FA-MONITOR-MIX-SPEC-006` | complete recent inputs | master trigger 時に全入力の recent valid frame が揃う場合だけ publish すること。 |
| `FA-MONITOR-MIX-TC-007` -> `FA-MONITOR-MIX-SPEC-007` | drop conditions | missing / stale / byte length mismatch / range overflow を publish せず counter に反映すること。 |
| `FA-MONITOR-MIX-TC-008` -> `FA-MONITOR-MIX-SPEC-008` | output metadata | output metadata が master frame と monitor output config から作られること。 |
| `FA-MONITOR-MIX-TC-009` -> `FA-MONITOR-MIX-SPEC-009` | non-goals | device API、resample、format conversion、limiter、normalize、silence synthesis、stale reuse を持たないこと。 |
| `FA-MONITOR-MIX-TC-010` -> `FA-MONITOR-MIX-SPEC-010` | diagnostics | frame counter、drop counter、backend / output state が diagnostics に含まれること。 |
| `FA-MONITOR-MIX-TC-011` -> `FA-MONITOR-MIX-SPEC-011` | launch contract | launch file が `node_name` / `config_file` だけを要求し、package default を推測しないこと。 |
| `FA-MONITOR-MIX-TC-012` -> `FA-MONITOR-MIX-SPEC-012` | colcon coverage | pytest と backend gTest が `colcon test` から実行されること。 |

## 1. 単体 contract test

Python の static contract test で、package-local source/config/launch が次を満たすことを確認する。

- default config が `fa_monitor_mix` key を使う
- launch default `node_name` が `fa_monitor_mix`
- `input_topics` と `input_stream_ids`、`output_topic` と `output.stream_id` を分け、topic identity と stream identity の衝突を fail closed する
- `FLOAT32LE` / `32` / `interleaved` 以外を起動時に拒否する
- 入力 frame の `source_id`、`stream_id`、format、data alignment、sample range を検証する
- missing / stale / byte length mismatch で mix を drop する
- output が `output.source_id` と `output_topic` を使う
- clamp / limiter / normalize / resample / device API を含まない
- diagnostics counter 名を publish する
- node core と `internal_monitor_mix` backend library が分かれている
- backend C++ test が input validation、gain mix、byte length mismatch、range overflow を確認する

## 2. C++ build test

親工程で `colcon build --packages-select fa_monitor_mix` を実行し、C++17 と依存関係の解決を確認する。

## 3. ROS2 smoke test

親工程で package を build 後、次を確認する。

1. default config で node が起動する。
2. `audio/program/frame` と `audio/tts/frame` に valid frame を publish すると
   `audio/monitor/frame` が publish される。
3. 片方の入力を止めると stale drop が増え、monitor frame が publish されない。
4. sample range 超過 frame を publish すると output は publish されず `range_drops` が増える。

## 4. 未検証扱いの境界

static test と py_compile だけでは ROS2 runtime の subscription/publish 動作は確認できない。
runtime smoke test まで通した場合だけ、ROS2 経路検証済みとして扱う。
