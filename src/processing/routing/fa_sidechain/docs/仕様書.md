# fa_sidechain 仕様書

## 1. 目的

`fa_sidechain` は、sidechain 音声 stream から明示的な gain-control stream を生成する routing / analysis bridge node である。

program audio の購読、変更、mix、ducking は行わない。下流 node が control stream を購読し、自身の責務として program audio へ反映する。

## 1.1. 仕様 ID

| ID | 内容 |
| --- | --- |
| `FA-SIDECHAIN-SPEC-001` | `fa_sidechain` は sidechain 音声 stream から明示的な gain-control stream を生成する routing / analysis bridge node である。 |
| `FA-SIDECHAIN-SPEC-002` | program audio の購読、変更、mix、ducking は行わず、downstream node が control stream を利用する。 |
| `FA-SIDECHAIN-SPEC-003` | `sidechain_topic` / `control_topic` と `sidechain_stream_id` / `control.stream_id` は別契約であり、topic identity と stream identity の衝突を起動時に拒否する。 |
| `FA-SIDECHAIN-SPEC-004` | detector threshold / gain、control sample rate、expected format、QoS、diagnostics parameter は required で、不正値は fail closed する。 |
| `FA-SIDECHAIN-SPEC-005` | sidechain input は `FLOAT32LE` / 32 bit / interleaved、expected sample rate / channels、finite normalized sample を満たす必要がある。 |
| `FA-SIDECHAIN-SPEC-006` | output `AudioFrame` は input header / source / epoch を保持し、`stream_id`、sample rate、channels、data は control contract に従う。 |
| `FA-SIDECHAIN-SPEC-007` | 不正 frame は publish せず drop し、値の補完、丸め、clamp、normalize を行わない。 |
| `FA-SIDECHAIN-SPEC-008` | RMS 計算、threshold 判定、dB to linear gain 変換、control payload bytes 生成は ROS-free backend に閉じる。 |
| `FA-SIDECHAIN-SPEC-009` | diagnostics は frame counter、last RMS、last gain、active state、設定値を publish する。 |
| `FA-SIDECHAIN-SPEC-010` | launch file は `node_name` / `config_file` を明示引数とし、package-local default config を推測しない。 |
| `FA-SIDECHAIN-SPEC-011` | pytest / backend gTest contract は `colcon test` から実行される。 |

## 2. 非目標

- program audio sample の変更
- resample
- normalize
- clamp
- compressor / limiter / ducking の実行
- 欠損 frame の補完
- 古い stream 名や旧 key の互換

## 3. ROS2 Interface

| direction | topic parameter | message |
| --- | --- | --- |
| subscribe | `sidechain_topic` | `fa_interfaces/msg/AudioFrame` |
| publish | `control_topic` | `fa_interfaces/msg/AudioFrame` |
| publish | `diagnostics` | `diagnostic_msgs/msg/DiagnosticArray` |

topic 名と `AudioFrame.stream_id` は別契約である。`sidechain_topic` / `control_topic` を stream ID として使ってはならない。

## 4. Parameters

すべて required であり、コード上に有効な runtime default はない。package launch の `node_name` と `config_file` も明示必須である。

| name | example | requirement |
| --- | --- | --- |
| `sidechain_topic` | `fa_sidechain/input` | 空文字不可、他 topic と distinct |
| `control_topic` | `fa_sidechain/control` | 空文字不可、`sidechain_topic` と別 topic |
| `sidechain_stream_id` | `audio/sidechain/frame` | 空文字不可、topic 名と一致不可 |
| `control.stream_id` | `audio/sidechain/control` | 空文字不可、topic 名および入力 stream と一致不可 |
| `detector.threshold_rms` | `0.05` | finite、`(0.0, 1.0]` |
| `detector.active_gain_db` | `-12.0` | finite、linear gain が `[0.0, 4.0]` |
| `detector.inactive_gain_db` | `0.0` | finite、linear gain が `[0.0, 4.0]` |
| `control.sample_rate` | `1000` | `> 0` |
| `expected.sample_rate` | `16000` | `> 0` |
| `expected.channels` | `1` | `> 0` |
| `expected.encoding` | `FLOAT32LE` | `FLOAT32LE` のみ |
| `expected.bit_depth` | `32` | `32` のみ |
| `expected.layout` | `interleaved` | `interleaved` のみ |
| `qos.depth` | `10` | `> 0` |
| `qos.reliable` | `false` | boolean |
| `diagnostics.qos.depth` | `10` | `> 0` |
| `diagnostics.qos.reliable` | `false` | boolean |
| `diagnostics.publish_period_ms` | `1000` | `> 0` |

startup config が不正な場合は例外で起動を停止する。

## 5. Input Validation

sidechain input frame は以下をすべて満たす必要がある。

- `source_id` が空でない
- `stream_id == sidechain_stream_id`
- `sample_rate == expected.sample_rate`
- `channels == expected.channels`
- `encoding == expected.encoding`
- `bit_depth == expected.bit_depth`
- `layout == expected.layout`
- `data` が空でない
- `data.size()` が `expected.channels * sizeof(float)` で割り切れる
- 全 sample が finite
- 全 sample が `[-1.0, 1.0]`

不正 frame は publish せず、`frames_dropped` を増やす。値の補完や丸めは行わない。

## 6. Output

出力 `AudioFrame` は次の通り。

- `header = input.header`
- `source_id = input.source_id`
- `stream_id = control.stream_id`
- `sample_rate = control.sample_rate`
- `channels = 1`
- `encoding = FLOAT32LE`
- `bit_depth = 32`
- `layout = interleaved`
- `epoch = input.epoch`
- `data = target_gain_linear` の float32 1 sample

`target_gain_linear` は finite かつ `[0.0, 4.0]` でなければならない。

## 7. Diagnostics

`diagnostics` には最低限以下を含める。

- `frames_in`
- `frames_out`
- `frames_dropped`
- `last_rms`
- `last_gain_linear`
- `last_active`
- `active_frames`
- `inactive_frames`

設定値も diagnostics に含め、実行中の control 契約を確認できるようにする。

## 8. backend 境界

RMS 計算、threshold 判定、dB to linear gain 変換、control payload bytes 生成は ROS-free backend `internal_sidechain_detector` に閉じる。backend は ROS2 topic、`fa_interfaces/msg/AudioFrame`、diagnostics、publisher/subscriber を知らない。

ROS node は topic subscription/publication、AudioFrame metadata validation、`stream_id` 更新、diagnostics を担当する。
