# fa_delay テスト設計

## 1. 対象

`fa_delay` の package layout、設定契約、C++ 実装上の境界検証、delay sample 変換、source reset、metadata 保持、diagnostics、colcon pytest 登録を対象とする。

## 1.1. traceability

| test ID | 対応仕様 | 観点 |
| --- | --- | --- |
| `FA-DELAY-TC-001` -> `FA-DELAY-SPEC-001` | delay boundary | intentional temporal delay を適用する processing node であること。 |
| `FA-DELAY-TC-002` -> `FA-DELAY-SPEC-002` | topic / stream identity | topic と stream ID を分離し、衝突を fail closed すること。 |
| `FA-DELAY-TC-003` -> `FA-DELAY-SPEC-003` | required startup config | delay、expected format、QoS、diagnostics parameter を required とすること。 |
| `FA-DELAY-TC-004` -> `FA-DELAY-SPEC-004` | runtime frame validation | source / stream identity、format、data alignment、sample range を検証すること。 |
| `FA-DELAY-TC-005` -> `FA-DELAY-SPEC-005` | delay sample conversion | `delay.ms` を whole samples へ変換し channel ごとに delay buffer を適用すること。 |
| `FA-DELAY-TC-006` -> `FA-DELAY-SPEC-006` | source reset | stream 先頭と source change で delay state を silence 初期化すること。 |
| `FA-DELAY-TC-007` -> `FA-DELAY-SPEC-007` | output metadata | output が `stream_id` と `data` だけを更新すること。 |
| `FA-DELAY-TC-008` -> `FA-DELAY-SPEC-008` | no hidden correction | invalid sample を clamp / normalize / 暗黙変換で補正しないこと。 |
| `FA-DELAY-TC-009` -> `FA-DELAY-SPEC-009` | diagnostics | delay 設定、source、counter が diagnostics に含まれること。 |
| `FA-DELAY-TC-010` -> `FA-DELAY-SPEC-010` | non-goals | device I/O、format conversion、echo、reverb、legacy alias を持たないこと。 |
| `FA-DELAY-TC-011` -> `FA-DELAY-SPEC-011` | launch contract | launch file が `node_name` / `config_file` だけを要求し、package default を推測しないこと。 |
| `FA-DELAY-TC-012` -> `FA-DELAY-SPEC-012` | colcon coverage | pytest / backend gTest と lint dependency が colcon manifest 上で成立すること。 |

## 2. 単体 contract test

`test/unit/test_fa_delay_audio_frame_contract.py` で以下を確認する。

- default config が topic と stream id を分離し、delay parameter、FLOAT32LE interleaved 契約を持つ
- package layout が標準構造と一致する
- 起動時パラメータが fail closed で範囲検証される
- `delay.ms` が `expected.sample_rate` から whole samples へ変換される
- runtime frame が source/stream/format/data size を検証される
- 不正 sample は backend が検出し、state mutation 前に drop される
- `stream_id == input_stream_id` を要求し、出力で `stream_id = output.stream_id` に更新する
- channel ごとの delay buffer が silence で初期化される
- `source_id` 変更で delay state が reset される
- device I/O、resampling、format conversion、channel conversion、normalize、limiter、filter、denoise、echo、reverb を含まない
- diagnostics に delay 設定、source、counters が含まれる
- backend gTest と `ament_cmake_pytest` で colcon からテストが実行される
- 未定義の backend status message は fallback 文字列へ丸めず例外にする

## 3. 統合テスト

現時点では統合テストは `.gitkeep` のみ配置する。ROS2 runtime を使う publish/subscribe 統合テストは、fa_interfaces と実行環境の fixture が整った段階で追加する。

## 4. 実行コマンド

```bash
env PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -p no:cacheprovider src/processing/temporal/fa_delay/test/unit -q
```
