# fa_silence_removal テスト設計

## 1. 目的

`fa_silence_removal` が silent chunk を publish せずに drop し、non-silent chunk と hangover chunk だけを元 metadata のまま publish することを確認する。

## 1.1. traceability

| test ID | 対応仕様 | 観点 |
| --- | --- | --- |
| `FA-SILENCE-REMOVAL-TC-001` -> `FA-SILENCE-REMOVAL-SPEC-001` | silence removal boundary | RMS で silent chunk を判定し publish しないこと。 |
| `FA-SILENCE-REMOVAL-TC-002` -> `FA-SILENCE-REMOVAL-SPEC-002` | no sample editing | silent chunk に zeroing、gain gate、noise gate を適用しないこと。 |
| `FA-SILENCE-REMOVAL-TC-003` -> `FA-SILENCE-REMOVAL-SPEC-003` | topic / stream identity | topic と stream ID を分離し、衝突を fail closed すること。 |
| `FA-SILENCE-REMOVAL-TC-004` -> `FA-SILENCE-REMOVAL-SPEC-004` | required startup config | threshold、hangover、expected format、QoS、diagnostics parameter を required とすること。 |
| `FA-SILENCE-REMOVAL-TC-005` -> `FA-SILENCE-REMOVAL-SPEC-005` | runtime frame validation | source / stream identity、format、data alignment、sample range を検証すること。 |
| `FA-SILENCE-REMOVAL-TC-006` -> `FA-SILENCE-REMOVAL-SPEC-006` | backend boundary | RMS 計算と hangover state を ROS-free backend に閉じること。 |
| `FA-SILENCE-REMOVAL-TC-007` -> `FA-SILENCE-REMOVAL-SPEC-007` | hangover policy | hangover 終端を chunk 境界仕様として扱うこと。 |
| `FA-SILENCE-REMOVAL-TC-008` -> `FA-SILENCE-REMOVAL-SPEC-008` | output metadata | publish frame が `stream_id` だけを更新し、`data` を書き換えないこと。 |
| `FA-SILENCE-REMOVAL-TC-009` -> `FA-SILENCE-REMOVAL-SPEC-009` | no hidden correction | invalid sample を clamp / normalize / zero 補完で補正しないこと。 |
| `FA-SILENCE-REMOVAL-TC-010` -> `FA-SILENCE-REMOVAL-SPEC-010` | diagnostics | threshold、hangover、drop counter が diagnostics に含まれること。 |
| `FA-SILENCE-REMOVAL-TC-011` -> `FA-SILENCE-REMOVAL-SPEC-011` | non-goals | device I/O、format conversion、noise gate、VAD / ASR、legacy alias を持たないこと。 |
| `FA-SILENCE-REMOVAL-TC-012` -> `FA-SILENCE-REMOVAL-SPEC-012` | launch contract | launch file が `node_name` / `config_file` だけを要求し、package default を推測しないこと。 |
| `FA-SILENCE-REMOVAL-TC-013` -> `FA-SILENCE-REMOVAL-SPEC-013` | colcon coverage | pytest / backend gTest と lint dependency が colcon manifest 上で成立すること。 |

## 2. 単体 contract tests

`test/unit/test_fa_silence_removal_audio_frame_contract.py` で以下を確認する。

- `config/default.yaml` が FLOAT32LE interleaved 契約を示す
- `input_topic` / `output_topic` と `input_stream_id` / `output.stream_id` を別概念として示す
- launch の `config_file` に package default がなく、明示設定が必須である
- `threshold.rms` と `hangover_ms` が明示されている
- 起動時設定が必須取得と範囲検証を通り、不正設定で fallback しない
- `hangover_ms` が `expected.sample_rate` から sample 数へ変換される
- node が frame contract を検証した後に backend へ進む
- RMS 計算と hangover state が ROS-free backend の責務である
- invalid sample を clamp / normalize せず、backend が rejection status を返す
- silent chunk branch が publish を呼ばない
- hangover branch が chunk を publish し、残量を減らす
- publish frame は `stream_id` のみ更新し、`data` を書き換えない
- diagnostics が threshold、hangover、stream identity、drop counters を出す
- colcon から pytest と backend gtest が実行される

## 3. C++ backend unit tests

`test/cpp/test_internal_rms_silence_removal_backend.cpp` で以下を確認する。

- active frame が accepted になり、hangover が reset される
- hangover 中の silent frame が accepted になり、その後 drop へ戻る
- empty / misaligned / NaN / out-of-range input が rejection status になる
- invalid input で backend state を commit しない
- invalid config が constructor で起動失敗相当の例外になる
- 未知の `Decision` / `ProcessStatus` が `std::logic_error` で fail closed する

## 4. C++ ビルド検証

代表コマンド:

```bash
timeout 300 colcon build --packages-select fa_silence_removal
```

確認観点:

- `fa_interfaces` に依存してビルドできる
- `diagnostic_msgs` の publisher が解決できる
- launch/config/install 対象が package share に入る

## 5. 統合テスト案

将来の integration test では、以下の fixture stream を使って ROS2 topic 経由で確認する。

| ケース | 入力 | 期待 |
| --- | --- | --- |
| non-silent | RMS が threshold 以上 | 1 message publish |
| silent | RMS が threshold 未満 | publish なし |
| active then silent in hangover | active 後、hangover 残量内の silent | silent も publish |
| active then silent after hangover | hangover 消費後の silent | publish なし |
| invalid sample | NaN / inf / out-of-range | publish なし、invalid counter 増加 |
| contract mismatch | encoding / channels / input_stream_id mismatch | publish なし、invalid counter 増加 |

## 6. launch test 案

`launch/fa_silence_removal.launch.py` を起動し、以下を確認する。

- `node_name` launch argument が node 名に反映される
- `config_file` launch argument で別 YAML を渡せる
- `config_file` 未指定では package default を使わず起動失敗になる
- 明示 config で node が起動する
- 不正 config では node が起動せず fatal log を出す

## 7. 未対象

この package の test では device I/O、resampling、ASR、VAD state publication、音声品質の主観評価を扱わない。
