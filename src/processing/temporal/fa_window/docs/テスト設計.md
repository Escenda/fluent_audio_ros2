# fa_window テスト設計

## traceability

| test ID | 対応仕様 | 観点 |
| --- | --- | --- |
| `FA-WINDOW-TC-001` -> `FA-WINDOW-SPEC-001` | window boundary | Hann / Hamming 解析窓を適用する temporal processing node であること。 |
| `FA-WINDOW-TC-002` -> `FA-WINDOW-SPEC-002` | non-goals | device I/O、format conversion、gain、limiter、filter、denoise を持たないこと。 |
| `FA-WINDOW-TC-003` -> `FA-WINDOW-SPEC-003` | topic / stream identity | topic と stream ID を分離し、衝突を fail closed すること。 |
| `FA-WINDOW-TC-004` -> `FA-WINDOW-SPEC-004` | required startup config | window type / frames、expected format、QoS、diagnostics parameter を required とすること。 |
| `FA-WINDOW-TC-005` -> `FA-WINDOW-SPEC-005` | runtime frame validation | source / stream identity、format、data alignment、sample range を検証すること。 |
| `FA-WINDOW-TC-006` -> `FA-WINDOW-SPEC-006` | frame count policy | strict true / false の frame count 挙動を明示仕様として扱うこと。 |
| `FA-WINDOW-TC-007` -> `FA-WINDOW-SPEC-007` | backend boundary | frame count policy、sample validation、window 適用を ROS-free backend に閉じること。 |
| `FA-WINDOW-TC-008` -> `FA-WINDOW-SPEC-008` | output metadata | output が `stream_id` と `data` だけを更新すること。 |
| `FA-WINDOW-TC-009` -> `FA-WINDOW-SPEC-009` | unknown status fail closed | unknown type / status を fallback 文字列へ丸めず fail closed すること。 |
| `FA-WINDOW-TC-010` -> `FA-WINDOW-SPEC-010` | diagnostics | window config、last frame count、counter が diagnostics に含まれること。 |
| `FA-WINDOW-TC-011` -> `FA-WINDOW-SPEC-011` | launch contract | launch file が `node_name` / `config_file` だけを要求し、package default を推測しないこと。 |
| `FA-WINDOW-TC-012` -> `FA-WINDOW-SPEC-012` | colcon coverage | pytest / backend gTest と lint dependency が colcon manifest 上で成立すること。 |

## Unit Contract Tests

`test/unit/test_fa_window_audio_frame_contract.py` は、実行環境に ROS2 node を起動せず、config と source を検査して package contract を固定する。

確認項目:

- example config が topic / stream identity と FLOAT32LE interleaved 契約を表す
- window が device I/O、resampling、format conversion、channel conversion、gain、limiter、filter、denoise を持たない
- 起動時 validation が fail closed になっている
- runtime frame validation が source/stream identity、format、data size を確認する
- strict frame count の true/false 挙動、Hann/Hamming 係数式、invalid sample drop を backend が担当する
- stream identity が `output.stream_id` に更新される
- diagnostics に設定値、stream identity、last frame count、counter が含まれる
- colcon pytest / backend gTest registration が存在する

## Integration / Launch

`test/integration` と `test/launch` は将来の ROS2 graph test 用の配置を確保する。現時点では `.gitkeep` のみを置く。
