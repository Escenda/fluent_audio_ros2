fa_notch:
  ros__parameters:
    input_topic: "fa_notch/input"
    output_topic: "fa_notch/output"
    input_stream_id: "audio/high_pass/mic"
    output:
      stream_id: "audio/notch/mic"

    filter:
      center_hz: 60.0
      q: 30.0

    expected:
      sample_rate: 16000
      channels: 1
      encoding: "FLOAT32LE"
      bit_depth: 32
      layout: "interleaved"

    qos:
      depth: 10
      reliable: false

    diagnostics:
      publish_period_ms: 1000
      qos:
        depth: 10
        reliable: true
