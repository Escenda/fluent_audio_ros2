# fa_normalize テスト設計

## 1. Python unit contract tests

`test/unit/test_fa_normalize_audio_frame_contract.py` は package source を検査し、以下を確認する。

- example config が `FLOAT32LE` / 32-bit / interleaved 契約を満たす
- ROS topic と `AudioFrame.stream_id` を分離し、衝突する設定を fail closed する
- `target_peak_linear` と `silence_threshold_linear` の値と範囲
- device I/O、resampling、format conversion、channel conversion、compressor、limiter、gate、filter、denoise を含まない
- 起動時設定不正を `std::runtime_error` で fail closed する
- runtime `AudioFrame` contract を処理前に検証する
- node が `NodeOptions` を受け取り、`main()` が `src/main.cpp` に分離されている
- `stream_id == input_stream_id` を要求し、出力 `stream_id` を `output.stream_id` に更新する
- peak/gain algorithm が backend にあり、node からは消えている
- silence threshold 未満では backend が data を pass-through する
- backend が ROS2 topic/message を知らない
- input/output sample の finite と normalized range を backend が検証し、clamp しない
- 拒否時は output vector と node の `last_gain` を commit しない
- diagnostics が target、threshold、last_gain、counter、topic/stream identity、QoS 設定を publish する
- runtime config / backend config が意味のある default を持たず、launch default や system default QoS へ fallback しない
- package layout と colcon pytest / backend gtest / ROS graph gtest registration が存在する

## 2. C++ backend tests

`test/cpp/test_internal_peak_normalize_backend.cpp` は ROS を使わず backend を直接検証する。

- 不正 config を constructor で拒否する
- valid frame の peak を target peak に正規化する
- silence frame は unity gain で pass-through する
- 空入力、misaligned input、非 finite input、範囲外 input を status で拒否する
- 拒否時に output vector を更新しない

## 3. ROS graph tests

`test/cpp/test_normalize_graph.cpp` は `NodeOptions` で `fa_normalize` を起動し、
ROS2 executor 上で publisher/subscriber を接続して以下を確認する。

- valid `FLOAT32LE` frame が publish される
- output frame は `source_id`、format metadata、`epoch` を保持し、`stream_id` を `output.stream_id` に更新する
- peak normalize により frame peak が `normalize.target_peak_linear` になる
- `stream_id` が `input_stream_id` と一致しない frame は publish されない

## 4. Launch tests

`test/launch/test_fa_normalize_launch_contract.py` は以下を確認する。

- launch argument が `node_name` / `config_file` のみに閉じている
- default config が dynamics node の責務に閉じ、resample / limiter / gain を含まない
- `normalize.target_peak_linear <= 0.0` で launch 経路が fail closed する
- `normalize.silence_threshold_linear >= normalize.target_peak_linear` で launch 経路が fail closed する
- wrong `expected.encoding` で launch 経路が fail closed する
- `input_stream_id` / `output.stream_id` が同一、または topic 名と衝突する設定で launch 経路が fail closed する

## 5. Manual validation command

```bash
env PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -p no:cacheprovider src/processing/dynamics/fa_normalize/test/unit src/processing/dynamics/fa_normalize/test/launch -q
```

## 6. Traceability

| TC ID | 対応 SPEC | 自動テスト | 種別 |
| --- | --- | --- | --- |
| `FA-NORMALIZE-TC-001` -> `FA-NORMALIZE-SPEC-001` | `FA-NORMALIZE-SPEC-001` | `test/unit/test_fa_normalize_audio_frame_contract.py::test_normalize_does_not_hide_other_processing_or_io_responsibilities` | static source contract |
| `FA-NORMALIZE-TC-002` -> `FA-NORMALIZE-SPEC-002` | `FA-NORMALIZE-SPEC-002` | `test/unit/test_fa_normalize_audio_frame_contract.py::test_example_config_requires_float32_interleaved_contract`, `test/unit/test_fa_normalize_audio_frame_contract.py::test_normalize_validates_frame_contract_before_processing` | static config/frame contract |
| `FA-NORMALIZE-TC-003` -> `FA-NORMALIZE-SPEC-003` | `FA-NORMALIZE-SPEC-003` | `test/unit/test_fa_normalize_audio_frame_contract.py::test_startup_rejects_invalid_config_without_fallback`, `test/unit/test_fa_normalize_audio_frame_contract.py::test_normalize_runtime_config_types_do_not_define_meaningful_defaults` | static startup contract |
| `FA-NORMALIZE-TC-004` -> `FA-NORMALIZE-SPEC-004` | `FA-NORMALIZE-SPEC-004` | `test/unit/test_fa_normalize_audio_frame_contract.py::test_peak_normalize_algorithm_is_backend_owned`, `test/cpp/test_internal_peak_normalize_backend.cpp` | static + backend runtime |
| `FA-NORMALIZE-TC-005` -> `FA-NORMALIZE-SPEC-005` | `FA-NORMALIZE-SPEC-005` | `test/unit/test_fa_normalize_audio_frame_contract.py::test_silence_pass_through_changes_only_stream_identity_and_does_not_amplify`, `test/cpp/test_internal_peak_normalize_backend.cpp` | static + backend runtime |
| `FA-NORMALIZE-TC-006` -> `FA-NORMALIZE-SPEC-006` | `FA-NORMALIZE-SPEC-006` | `test/unit/test_fa_normalize_audio_frame_contract.py::test_normalize_preserves_frame_identity_and_updates_stream_identity` | static frame contract |
| `FA-NORMALIZE-TC-007` -> `FA-NORMALIZE-SPEC-007` | `FA-NORMALIZE-SPEC-007` | `test/unit/test_fa_normalize_audio_frame_contract.py::test_normalize_validates_frame_contract_before_processing`, `test/cpp/test_normalize_graph.cpp::DropsFrameWhenStreamIdDoesNotMatchInputTopic` | static + ROS graph smoke |
| `FA-NORMALIZE-TC-008` -> `FA-NORMALIZE-SPEC-008` | `FA-NORMALIZE-SPEC-008` | `test/unit/test_fa_normalize_audio_frame_contract.py::test_normalize_validates_frame_contract_before_processing` | static frame contract |
| `FA-NORMALIZE-TC-009` -> `FA-NORMALIZE-SPEC-009` | `FA-NORMALIZE-SPEC-009` | `test/unit/test_fa_normalize_audio_frame_contract.py::test_package_layout_matches_standard_processing_layout`, `test/launch/test_fa_normalize_launch_contract.py::test_launch_uses_only_node_name_and_config_file_arguments` | layout + launch contract |
| `FA-NORMALIZE-TC-010` -> `FA-NORMALIZE-SPEC-010` | `FA-NORMALIZE-SPEC-010` | `test/cpp/test_normalize_graph.cpp::PublishesPeakNormalizedFloat32Frame`, `test/cpp/test_normalize_graph.cpp::DropsFrameWhenStreamIdDoesNotMatchInputTopic` | ROS graph smoke |
| `FA-NORMALIZE-TC-011` -> `FA-NORMALIZE-SPEC-011` | `FA-NORMALIZE-SPEC-011` | `test/launch/test_fa_normalize_launch_contract.py`, `test/unit/test_fa_normalize_audio_frame_contract.py::test_diagnostics_qos_is_explicit_and_not_system_defaulted` | launch + static diagnostics contract |
