fa_noise_gate:
  ros__parameters:
    input_topic: "fa_noise_gate/input"
    output_topic: "fa_noise_gate/output"
    input_stream_id: "audio/dc_offset_removed/mic"

    output:
      stream_id: "audio/noise_gated/mic"

    gate:
      threshold_linear: 0.02
      closed_gain_linear: 0.0

    expected:
      sample_rate: 16000
      channels: 1
      encoding: "FLOAT32LE"
      bit_depth: 32
      layout: "interleaved"

    qos:
      depth: 10
      reliable: false

    diagnostics:
      qos:
        depth: 10
        reliable: false
      publish_period_ms: 1000
