# fa_noise_gate テスト設計

## 1. 方針

unit / backend / ROS graph / launch tests は、起動設定、runtime contract、backend 境界、
algorithm behavior、publish/drop、fail-closed 起動を executable に確認する。

## 2. 対象

| テスト | 確認内容 |
| --- | --- |
| example config | topic / stream identity、gate parameter、expected FLOAT32LE interleaved contract |
| unrelated responsibilities | device I/O、resample、format conversion、limiter、normalize、filter、denoise を含まない |
| startup validation | gate parameter と expected contract が fail closed で検証される |
| frame validation | source/stream、`input_stream_id` policy、format、data size を処理前に検証する |
| identity update | 入力 frame を preserve し、`output.stream_id` と data だけを明示更新する |
| topic/stream split | ROS topic と `AudioFrame.stream_id` を分離し、衝突する設定を fail closed する |
| diagnostics QoS | diagnostics QoS を明示 parameter として扱い、system QoS fallback に依存しない |
| algorithm evidence | backend が `abs(sample) < threshold` のとき closed gain、その他 preserve |
| invalid sample handling | backend が非有限・範囲外 input/output を `ProcessStatus` で返し、clamp しない |
| backend boundary | backend が `rclcpp` / `fa_interfaces` / `AudioFrame` を知らない |
| node core | node が `NodeOptions` を受け取り、`main()` が `src/main.cpp` に分離されている |
| executable coverage | backend API、ROS graph、launch fail-closed を検証する |

## 3. Backend C++ tests

- threshold 以上の sample を preserve する。
- threshold 未満の sample に closed gain を適用し、gated sample count を返す。
- `closed_gain_linear=0.0` を明示 mute として扱う。
- empty / byte alignment / NaN / normalized range violation を `ProcessStatus` で返す。
- rejected frame では output buffer を上書きしない。

## 4. ROS graph tests

`test/cpp/test_noise_gate_graph.cpp` は `NodeOptions` で `fa_noise_gate` を起動し、ROS2 executor 上で publisher/subscriber を接続する。

- valid frame が publish されること
- threshold 未満 sample に `closed_gain_linear` が適用されること
- `source_id`、format、`epoch` が保持され、`stream_id` が `output.stream_id` に更新されること
- `stream_id != input_stream_id` の frame が publish されないこと

## 5. Launch tests

`test/launch/test_fa_noise_gate_launch_contract.py` は launch file と example config を検査し、ROS2 が利用可能な環境では不正 config の起動失敗を確認する。

- launch argument は `node_name` / `config_file` のみ
- example config は noise gate 専用の dynamics node として resample / normalize / compressor / limiter を含まない
- `gate.threshold_linear < 0.0` は起動失敗
- `gate.closed_gain_linear > 1.0` は起動失敗
- `expected.encoding != FLOAT32LE` は起動失敗
- stream identity 衝突、stream identity と ROS topic の衝突は起動失敗

## 6. 実行コマンド

```bash
env PYTHONDONTWRITEBYTECODE=1 PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest -p no:cacheprovider src/processing/dynamics/fa_noise_gate/test/unit src/processing/dynamics/fa_noise_gate/test/launch -q
```

## 7. Traceability

| TC ID | 対応 SPEC | 自動テスト | 種別 |
| --- | --- | --- | --- |
| `FA-NOISE-GATE-TC-001` -> `FA-NOISE-GATE-SPEC-001` | `FA-NOISE-GATE-SPEC-001` | behavior checks verify no unrelated processing or I/O capability | behavior boundary |
| `FA-NOISE-GATE-TC-002` -> `FA-NOISE-GATE-SPEC-002` | `FA-NOISE-GATE-SPEC-002` | config and launch validation require explicit `FLOAT32LE/32/interleaved` and reject invalid config | launch/config fail-closed |
| `FA-NOISE-GATE-TC-003` -> `FA-NOISE-GATE-SPEC-003` | `FA-NOISE-GATE-SPEC-003` | backend API verifies threshold and closed gain behavior | backend API |
| `FA-NOISE-GATE-TC-004` -> `FA-NOISE-GATE-SPEC-004` | `FA-NOISE-GATE-SPEC-004` | runtime validation and ROS graph test reject mismatched stream identity or unsupported frame metadata before publish | frame/graph rejection |
| `FA-NOISE-GATE-TC-005` -> `FA-NOISE-GATE-SPEC-005` | `FA-NOISE-GATE-SPEC-005` | ROS graph behavior preserves frame identity and updates stream identity | ROS graph behavior |
| `FA-NOISE-GATE-TC-006` -> `FA-NOISE-GATE-SPEC-006` | `FA-NOISE-GATE-SPEC-006` | backend API rejects invalid samples without clamp/normalize fallback and without overwriting output | backend rejection |
| `FA-NOISE-GATE-TC-007` -> `FA-NOISE-GATE-SPEC-007` | `FA-NOISE-GATE-SPEC-007` | diagnostics behavior publishes config and counters | diagnostics behavior |
| `FA-NOISE-GATE-TC-008` -> `FA-NOISE-GATE-SPEC-008` | `FA-NOISE-GATE-SPEC-008` | `test/launch/test_fa_noise_gate_launch_contract.py::test_launch_uses_only_node_name_and_config_file_arguments`, `test/launch/test_fa_noise_gate_launch_contract.py` | launch contract |
| `FA-NOISE-GATE-TC-009` -> `FA-NOISE-GATE-SPEC-009` | `FA-NOISE-GATE-SPEC-009` | ROS graph test starts the node with explicit parameters and publishes gated `FLOAT32LE` frames | ROS graph behavior |
| `FA-NOISE-GATE-TC-010` -> `FA-NOISE-GATE-SPEC-010` | `FA-NOISE-GATE-SPEC-010` | `test/launch/test_fa_noise_gate_launch_contract.py::test_launch_fails_closed_when_stream_ids_are_not_distinct`, `test/launch/test_fa_noise_gate_launch_contract.py::test_launch_fails_closed_when_stream_id_matches_topic`, `test/launch/test_fa_noise_gate_launch_contract.py::test_launch_fails_closed_when_output_stream_id_matches_topic` | launch contract |
| `FA-NOISE-GATE-TC-011` -> `FA-NOISE-GATE-SPEC-011` | `FA-NOISE-GATE-SPEC-011` | diagnostics behavior uses explicit QoS and no hidden fallback patterns | diagnostics behavior |
