# fa_tts テスト設計

## unit test

- empty text を拒否する。
- cache key が text / voice で変わる。
- waveform scale normalization。
- `request.play=true` を拒否する。
- `request.volume_db != 0.0` を拒否する。

## integration test

- `Speak` service から `audio/tts/frame` が publish される。
- `fa_tts -> fa_mix -> fa_out` の routing で playback される。

## launch test

- `config/default.yaml` で起動できる環境では起動する。
- pyopenjtalk がない環境では起動失敗する。

## failure test

- synthesis failure。
- cache directory write failure。
- invalid voice。

## latency test

cache miss / cache hit の service latency を分けて測る。

## real device smoke test

`fa_tts -> fa_mix -> fa_out` で実 speaker から音声が出ること、stop 後の古い epoch が後段で破棄されることを確認する。
