# fa_neural_vocoder

Roadmap directory for the generation processing node that reconstructs waveform
audio from acoustic features such as mel spectrograms.

This is not a ROS 2 package yet. Do not add `package.xml` until the package has
its specification, algorithm notes, backend documentation, launch contract, and
tests.
