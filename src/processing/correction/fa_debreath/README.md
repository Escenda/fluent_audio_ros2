# fa_debreath

Roadmap directory for the correction processing node that suppresses breath
noise without changing source or sink adapter responsibilities.

This is not a ROS 2 package yet. Do not add `package.xml` until the package has
its specification, algorithm notes, backend documentation, launch contract, and
tests.
