fa_dc_offset_removal:
  ros__parameters:
    input_topic: "fa_dc_offset_removal/input"
    output_topic: "fa_dc_offset_removal/output"
    input_stream_id: "audio/sample_format/mic"

    expected:
      sample_rate: 16000
      channels: 1
      encoding: "FLOAT32LE"
      bit_depth: 32
      layout: "interleaved"

    output:
      stream_id: "audio/dc_offset_removed/mic"

    qos:
      depth: 10
      reliable: false

    diagnostics:
      publish_period_ms: 1000
      qos:
        depth: 10
        reliable: true
