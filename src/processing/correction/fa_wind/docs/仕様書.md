# fa_wind 仕様書

## 1. 目的

`fa_wind` は `FLOAT32LE` interleaved `AudioFrame` の wind noise を低減する correction processing node である。
風切り音の検出・抑制 contract は config で明示し、欠損時に high-pass や gate へ暗黙 fallback しない。

## 2. 入力/出力

入力と出力は同じ sample rate、channels、encoding、bit depth、layout を持つ `AudioFrame` である。
`source_id` は保持し、`stream_id` は `output_topic` に更新する。

## 3. 責務外

- high-pass filter
- noise gate / expander
- beamforming
- device / file / network I/O
- resample / channel conversion / bit-depth conversion
- wind profile 欠損時の pass-through

## 4. fail closed 条件

- backend 未指定
- wind detector / suppression config 欠損
- input metadata mismatch
- non-finite sample
- output が finite/normalized contract を満たさない

