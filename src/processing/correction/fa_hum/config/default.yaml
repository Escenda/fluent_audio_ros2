fa_hum:
  ros__parameters:
    input_topic: "audio/dc_offset_removed/mic"
    output_topic: "audio/hum_removed/mic"

    hum:
      frequency_hz: 60.0
      harmonics: 4
      q: 30.0

    expected:
      sample_rate: 16000
      channels: 1
      encoding: "FLOAT32LE"
      bit_depth: 32
      layout: "interleaved"

    qos:
      depth: 10
      reliable: false

    diagnostics:
      publish_period_ms: 1000
