fa_aec_nn:
  ros__parameters:
    enabled: true
    # Required. Use "passthrough" only for explicit wiring/debug validation.
    backend.name: ""

    input_topic: "fa_aec_nn/input"
    output_topic: "fa_aec_nn/output"
    input_stream_id: "audio/aec_linear/frame"
    output:
      stream_id: "audio/aec/frame"

    expected_sample_rate: 16000
    expected_channels: 1
    expected:
      encoding: "PCM16LE"
      bit_depth: 16

    qos:
      depth: 10
      reliable: false

    diagnostics:
      publish_period_ms: 1000
      qos:
        depth: 10
        reliable: true
