# fa_stream 仕様書

## 目的

`fa_stream` は `AudioFrame` stream を network / radio style stream へ渡す sink utility である。device playback ではなく、外部監視・配信・遠隔操作向けの出力経路を担う。

この package は `src/io/utilities` に置く。`src/streaming` は jitter buffer、clock drift correction、packet loss concealment、time alignment などのリアルタイム伝送安定化 node の置き場であり、network endpoint へ送出する sink utility の置き場ではない。

## 仕様 ID

| ID | 内容 |
| --- | --- |
| `FA-STREAM-SPEC-001` | `fa_stream` は `AudioFrame` stream を network / radio style stream へ渡す sink utility であり、device playback や realtime transport correction node ではない。 |
| `FA-STREAM-SPEC-002` | `input_topic`、`ffmpeg_path`、`output_url`、codec / container / content type / loglevel は起動時に明示設定され、endpoint 欠落は起動失敗にする。 |
| `FA-STREAM-SPEC-003` | QoS は system config で用途ごとに決め、node が device や network 状態から暗黙に変更しない。 |
| `FA-STREAM-SPEC-004` | endpoint 未設定、codec 不一致、network 接続失敗、write failure は明示 error とし、別 endpoint や local file へ fallback しない。 |
| `FA-STREAM-SPEC-005` | 入力 `AudioFrame` は `layout=interleaved`、`encoding=PCM16LE`、`bit_depth=16`、正の sample rate / channels、非空 data を満たす。 |
| `FA-STREAM-SPEC-006` | ROS2 node は topic / parameter / `AudioFrame` validation のみを担当し、ffmpeg process 管理は ROS2 非依存の backend に閉じる。 |
| `FA-STREAM-SPEC-007` | `audio_codec` / `container_format` は sink-local protocol packaging 設定であり、汎用 encode/decode や sample rate / channel / bit depth 変換を行わない。 |
| `FA-STREAM-SPEC-008` | `fa_stream` は microphone capture、resampling、codec 自動判定、jitter buffer、packet loss concealment を行わない。 |
| `FA-STREAM-SPEC-009` | launch file は `node_name` / `config_file` を明示引数とし、package-local default config を推測しない。 |
| `FA-STREAM-SPEC-010` | pytest contract は `colcon test` から実行される。 |

## 入力 topic

| topic | type | 内容 |
| --- | --- | --- |
| `input_topic` | `fa_interfaces/msg/AudioFrame` | 配信対象の音声 frame |

## 出力 topic

なし。出力は backend が管理する network stream である。

## parameters

| parameter | 意味 |
| --- | --- |
| `input_topic` | 購読する `AudioFrame` topic |
| `ffmpeg_path` | 実行する ffmpeg binary |
| `output_url` | stream endpoint。必須 |
| `audio_codec` | ffmpeg の audio codec |
| `bitrate` | audio bitrate |
| `container_format` | ffmpeg output format |
| `content_type` | stream content type |
| `loglevel` | ffmpeg loglevel |

## lifecycle

起動時に `input_topic` を検証し、stream backend を初期化して subscription を作る。stream endpoint が未設定の場合は起動失敗にする。

## QoS

streaming は欠落より遅延を優先する場合がある。QoS は system config で用途ごとに決め、node が device や network 状態から暗黙に変更しない。

## error handling

endpoint 未設定、codec 不一致、network 接続失敗、write failure は明示 error とする。別 endpoint や local file への暗黙 fallback はしない。

入力 `AudioFrame` は `layout=interleaved`、`encoding=PCM16LE`、`bit_depth=16`、正の `sample_rate` / `channels`、非空 data を満たす必要がある。`fa_stream` は ffmpeg stdin に raw `s16le` として渡す sink utility なので、encoding mismatch を bit depth だけで吸収しない。PCM16BE や FLOAT32LE などは前段の format node で明示変換してから渡す。

## backend selection

network streamer backend を使用する。backend は validated audio chunk と endpoint config を受け取る。

ROS2 node は topic / parameter / `AudioFrame` validation のみを担当する。`ffmpeg` process の起動、stdin write、停止処理は ROS2 非依存の `NetworkStreamerBackend` に閉じる。

`audio_codec` / `container_format` は network endpoint protocol packaging のための sink-local 設定である。汎用的な encode/decode や sample rate / channel / bit depth 変換は `fa_stream` の責務ではない。事前に別 codec 表現へ変換したい場合は `src/processing/format` の encode / format node を pipeline に明示的に挟む。

## non-goals

- microphone capture
- resampling
- codec 自動判定
- jitter buffer
- packet loss concealment
