# fa_network_in テスト設計

## Unit Test

- `FA-NETWORK-IN-SPEC-001`: package 化前でも README、仕様書、アルゴリズム詳細説明書、テスト設計、backend docs が存在する。
- `FA-NETWORK-IN-SPEC-002`: `package.xml` と `CMakeLists.txt` を package 化前に置かない。
- `FA-NETWORK-IN-SPEC-003`: 仕様書が jitter / PLC / drift correction を責務外にする。

## Integration Test

ROS2 package 化後に、fake receiver backend で packet contract と missing endpoint failure を確認する。

## Launch Test

ROS2 package 化後に、missing params file、missing backend、missing endpoint で起動失敗することを確認する。

