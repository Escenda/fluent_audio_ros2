fa_network_out:
  ros__parameters:
    backend.name: network_pcm_sender
    endpoint.uri: ""
    transport.identity: ""
    input_topic: audio/network_out
    expected:
      sample_rate: 16000
      channels: 1
      encoding: PCM16LE
      bit_depth: 16
      layout: interleaved
    qos:
      depth: 10
      reliable: true
    diagnostics:
      publish_period_ms: 1000
