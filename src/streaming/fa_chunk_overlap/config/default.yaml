fa_chunk_overlap:
  ros__parameters:
    input_topic: "audio/float32le/mic"
    output_topic: "audio/chunked_overlap/mic"
    expected:
      sample_rate: 16000
      channels: 1
      encoding: "FLOAT32LE"
      bit_depth: 32
      layout: "interleaved"
    window:
      frame_samples: 512
      hop_samples: 256
    qos:
      depth: 10
      reliable: true
    diagnostics:
      publish_period_ms: 1000
