# fa_jitter_buffer テスト設計

## 方針

初期実装では、pytest による text / contract test と、C++ gtest による ROS2 pub/sub contract test を置く。package-local integration / launch contract test は実行可能な pytest として置き、より長時間の reconnect / transport integration は未検証項目として分離する。

## Unit contract tests

| TC ID | 対応 SPEC | 観点 |
| --- | --- | --- |
| `FA-JITTER-BUFFER-TC-001` -> `FA-JITTER-BUFFER-SPEC-001` | `FA-JITTER-BUFFER-SPEC-001` | required parameter が明示されていること |
| `FA-JITTER-BUFFER-TC-002` -> `FA-JITTER-BUFFER-SPEC-002` | `FA-JITTER-BUFFER-SPEC-002` | 標準 streaming layout を満たすこと |
| `FA-JITTER-BUFFER-TC-003` -> `FA-JITTER-BUFFER-SPEC-003` | `FA-JITTER-BUFFER-SPEC-003` | required parameter にコード default を持たず、invalid config を例外にすること |
| `FA-JITTER-BUFFER-TC-004` -> `FA-JITTER-BUFFER-SPEC-004` | `FA-JITTER-BUFFER-SPEC-004` | invalid frame と `stream_id != input_stream_id` を buffer mutation 前に drop すること |
| `FA-JITTER-BUFFER-TC-005` -> `FA-JITTER-BUFFER-SPEC-005` | `FA-JITTER-BUFFER-SPEC-005` | `FLOAT32LE` + `interleaved` で finite / normalized sample を検証すること |
| `FA-JITTER-BUFFER-TC-006` -> `FA-JITTER-BUFFER-SPEC-006` | `FA-JITTER-BUFFER-SPEC-006` | `std::map` で epoch 昇順 publish すること |
| `FA-JITTER-BUFFER-TC-007` -> `FA-JITTER-BUFFER-SPEC-007` | `FA-JITTER-BUFFER-SPEC-007` | duplicate と publish 済みより古い epoch を drop すること |
| `FA-JITTER-BUFFER-TC-008` -> `FA-JITTER-BUFFER-SPEC-008` | `FA-JITTER-BUFFER-SPEC-008` | source / format change と configured epoch regression reset を明示的に扱うこと |
| `FA-JITTER-BUFFER-TC-009` -> `FA-JITTER-BUFFER-SPEC-009` | `FA-JITTER-BUFFER-SPEC-009` | config、buffered count、counter を publish すること |
| `FA-JITTER-BUFFER-TC-010` -> `FA-JITTER-BUFFER-SPEC-010` | `FA-JITTER-BUFFER-SPEC-010` | `ament_cmake_gtest`、`ament_cmake_pytest`、`ament_lint_auto`、pytest/yaml deps を持つこと |
| `FA-JITTER-BUFFER-TC-011` -> `FA-JITTER-BUFFER-SPEC-011` | `FA-JITTER-BUFFER-SPEC-011` | integration contract が data-plane config と runtime source contract を検証する |
| `FA-JITTER-BUFFER-TC-012` -> `FA-JITTER-BUFFER-SPEC-012` | `FA-JITTER-BUFFER-SPEC-012` | launch contract が node_name / config_file のみを公開する |
| `FA-JITTER-BUFFER-TC-013` -> `FA-JITTER-BUFFER-SPEC-013` | `FA-JITTER-BUFFER-SPEC-013` | ROS graph contract が epoch 昇順 publish、invalid sample drop、duplicate drop、source contract reset を検証する |
| `FA-JITTER-BUFFER-TC-014` -> `FA-JITTER-BUFFER-SPEC-014` | `FA-JITTER-BUFFER-SPEC-014` | resample、format conversion、gain、padding、PLC、device/network I/O、legacy alias を非目標にする |

## Integration / launch contract tests

`test/integration/test_fa_jitter_buffer_streaming_config_contract.py` で以下を確認する。

- default config が明示的な input / output topic、stream identity、QoS、diagnostics contract を持つ
- runtime source が `AudioFrame`、validation、diagnostics を含む

`test/launch/test_fa_jitter_buffer_streaming_launch_contract.py` で以下を確認する。

- launch file は `node_name` と `config_file` だけを公開する
- package / executable が `fa_jitter_buffer` の契約に一致し、config path は launch argument で明示する
- streaming node の launch に `backend.name` を漏らさない

## ROS graph contract tests

`test/cpp/test_fa_jitter_buffer_node_contract.cpp` で実 ROS2 pub/sub を使い、次を確認する。

- `input_topic` へ `stream_id == input_stream_id` の `AudioFrame` を publish したとき、`output_topic` へ `stream_id == output.stream_id` で epoch 昇順に出ること。
- finite / normalized ではない `FLOAT32LE` sample は output に出ないこと。
- publish 済み epoch の duplicate は output に再送されないこと。
- source contract change では buffered frame を reset し、別 source と混ぜないこと。

## 未検証の ROS graph integration

今後の長時間 integration test では次を確認する。

- invalid frame が diagnostics counter に反映されること。
- reconnect / launch parameter mismatch 時に fail closed すること。
