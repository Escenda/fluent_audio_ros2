fa_frame_buffer:
  ros__parameters:
    input_topic: "fa_frame_buffer/input"
    output_topic: "fa_frame_buffer/output"
    input_stream_id: "audio/noise_gated/mic"

    output:
      stream_id: "audio/buffered/mic"

    expected:
      sample_rate: 16000
      channels: 1
      encoding: "FLOAT32LE"
      bit_depth: 32
      layout: "interleaved"

    buffering:
      frames_per_chunk: 512
      max_buffered_chunks: 4

    qos:
      depth: 10
      reliable: false

    diagnostics:
      publish_period_ms: 1000
      qos:
        depth: 10
        reliable: true
