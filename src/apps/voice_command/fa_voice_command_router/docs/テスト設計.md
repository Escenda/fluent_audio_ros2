# fa_voice_command_router テスト設計

## traceability

| test ID | 対応仕様 | 観点 |
| --- | --- | --- |
| `FA-VOICE-COMMAND-ROUTER-TC-001` -> `FA-VOICE-COMMAND-ROUTER-SPEC-003` | required startup config | `config/default.yaml` が全必須 parameter を明示し、node が runtime default を持たないこと。 |
| `FA-VOICE-COMMAND-ROUTER-TC-002` -> `FA-VOICE-COMMAND-ROUTER-SPEC-004` | allowed mode validation | 初期 `mode` が `allowed_modes` にない場合、起動時に拒否すること。 |
| `FA-VOICE-COMMAND-ROUTER-TC-003` -> `FA-VOICE-COMMAND-ROUTER-SPEC-003` | typed parameters | `bool()` / `str()` cast や曖昧型で parameter 型違いを吸収しないこと。 |
| `FA-VOICE-COMMAND-ROUTER-TC-004` -> `FA-VOICE-COMMAND-ROUTER-SPEC-001` | canonical command schema | `start` / `stop` / `mode <name>` / `status` だけを受理し、互換 alias を持たないこと。 |
| `FA-VOICE-COMMAND-ROUTER-TC-005` -> `FA-VOICE-COMMAND-ROUTER-SPEC-002` | state and stop outputs | command topic 入力で state topic が更新され、stop command で output stop が publish されること。 |
| `FA-VOICE-COMMAND-ROUTER-TC-006` -> `FA-VOICE-COMMAND-ROUTER-SPEC-007` | explicit command errors | 不明 command、未許可 mode、TTS unavailable を成功扱いせず、`standby` に置き換えないこと。 |
| `FA-VOICE-COMMAND-ROUTER-TC-007` -> `FA-VOICE-COMMAND-ROUTER-SPEC-008` | no TTS backend ownership | router が TTS playback / gain parameter を持たず、`Speak` request を `play=false`、`volume_db=0.0` で発行すること。 |
| `FA-VOICE-COMMAND-ROUTER-TC-008` -> `FA-VOICE-COMMAND-ROUTER-SPEC-010` | launch contract | launch file が `node_name` / `config_file` だけを要求し、package-local default config を推測しないこと。 |
| `FA-VOICE-COMMAND-ROUTER-TC-009` -> `FA-VOICE-COMMAND-ROUTER-SPEC-011` | colcon coverage | package の pytest contract が `colcon test` から実行されること。 |
| `FA-VOICE-COMMAND-ROUTER-TC-010` -> `FA-VOICE-COMMAND-ROUTER-SPEC-009` | no audio processing | router が audio frame processing や ASR / KWS / VAD / TTS waveform generation を行わないこと。 |
| `FA-VOICE-COMMAND-ROUTER-TC-011` -> `FA-VOICE-COMMAND-ROUTER-SPEC-005` | runtime update boundary | startup-only parameter の runtime update を拒否すること。 |

## unit test

- `allowed_modes` にない初期 mode を拒否する。
- command から期待する state へ遷移する。
- 不明 command を成功扱いにしない。
- node 実装に runtime default を置かず、`config/default.yaml` が全必須 parameter を明示する。
- `bool()` / `str()` cast で parameter 型違いを吸収しない。

## integration test

- command topic 入力で state topic が更新される。
- command schema は `start` / `stop` / `mode <name>` / `status` のみを受理し、互換 alias を持たない。
- `announce_tts=true` のとき `Speak` service request が発行される。
- stop command で `output_stop_topic` が publish される。

## launch test

- `config/default.yaml` で起動できる。
- missing `tts_service` や空 topic を fail closed できる。
- `ament_add_pytest_test` により package 配下の pytest が `colcon test` で実行される。

## failure test

- TTS service unavailable を検出する。
- invalid mode を暗黙変換しない。
- startup-only parameter の runtime update を拒否する。

## latency test

command 受信から state publish までの control-plane latency を測る。

## real device smoke test

ASR result から command bridge を経由し、router state と TTS announcement が robot session で観測できることを確認する。
