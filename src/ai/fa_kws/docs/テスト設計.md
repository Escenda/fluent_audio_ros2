# fa_kws テスト設計

## Unit Test

- `FA-KWS-SPEC-001`: backend config validation。
- `FA-KWS-SPEC-002`: AudioFrame canonical format validation。
- `FA-KWS-SPEC-003`: VAD gate threshold。
- `FA-KWS-SPEC-004`: worker detection result mapping。
- `FA-KWS-SPEC-005`: `backend.execution_provider` missing / unsupported validation。
- `FA-KWS-SPEC-006`: VAD state missing / stale frame skip。
- `FA-KWS-SPEC-007`: `channels != 1` / `sample_rate != target_sample_rate` / `encoding != FLOAT32LE` / `bit_depth != 32` を reject し、node 内変換しない。
- `FA-KWS-SPEC-008`: normalized range 外または non-finite samples を reject する。
- `FA-KWS-SPEC-009`: `kws_wav_tool` は non-canonical WAV を resample/downmix/bit-depth convert せず reject する。
- `FA-KWS-SPEC-010`: `vad.probability_gate` は `(0.0, 1.0]` の有限値だけを許可し、backend / wav tool に VAD gate bypass を残さない。
- `FA-KWS-SPEC-011`: VAD gate 未満では node が backend を呼ばず、backend も sherpa-onnx ingest 前に gate を評価する。
- `FA-KWS-SPEC-012`: `kws_wav_tool` は `--assume-vad-speech` なしで起動しない。
- `FA-KWS-SPEC-013`: VAD probability / gate threshold / inclusive gate の pure contract は C++ gtest で実行時検証する。
- `FA-KWS-SPEC-014`: launch は `node_name` と `config_file` を必須で受け取り、backend/model/provider fallback を埋め込まない。
- `FA-KWS-SPEC-015`: sherpa-onnx backend は node / wav tool に直接重複コンパイルせず、`fa_kws_backends` library としてリンクする。
- `FA-KWS-SPEC-016`: `fa_kws_node` / `fa_kws_wav_tool` は sherpa-onnx C API に link せず、runtime は external worker command に閉じる。
- `FA-KWS-SPEC-017`: `include/fa_kws/backends` と `src/backends` の全 backend source を走査し、ROS2 topic/message 依存が入らないことを確認する。
- `FA-KWS-SPEC-018`: node は debug dump / WAV encode / PCM16 conversion を持たず、記録は `fa_record` へ分離する。
- `FA-KWS-SPEC-019`: `PCM32LE/32` payload は float32 として解釈せず、実行テストで reject を確認する。
- `FA-KWS-SPEC-020`: empty `AudioFrame.data` は silent no-detection ではなく invalid frame として reject する。
- `FA-KWS-SPEC-021`: backend public boundary は empty samples / sample-rate mismatch / missing stream を no-detection にせず fail closed する。
- `FA-KWS-SPEC-022`: backend constructor は model path と numeric config を C API 初期化前に検証する。
- `FA-KWS-SPEC-023`: `expected_source_id` と `expected_stream_id` により `AudioFrame.source_id` / `stream_id` を bind し、不一致 frame を reject する。
- `FA-KWS-SPEC-024`: `VadState.source_id` / `stream_id` が `expected_source_id` / `expected_stream_id` と一致しない場合は fresh VAD state として扱わない。
- `FA-KWS-SPEC-025`: node / backend config struct は hidden runtime default を持たず、required ROS parameter 欠落時は起動失敗する。
- `FA-KWS-SPEC-026`: `kws_wav_tool` は backend numeric config を CLI で明示させ、`SherpaOnnxKwsBackendConfig` 全 field を初期化してから backend を生成する。
- `FA-KWS-SPEC-027`: `expected_stream_id` は ROS topic 名と衝突してはならない。
- `FA-KWS-SPEC-028`: audio / VAD / output QoS は required ROS parameter とし、node code の `KeepLast(10/20)` や fixed reliability を禁止する。
- `FA-KWS-SPEC-029`: `backend.command` / `backend.args` / `backend.health_args` / `backend.timeout_sec` / `backend.workspace_dir` は必須で、worker health check failure は起動失敗する。
- `FA-KWS-SPEC-030`: `scripts/sherpa_onnx_kws_worker` は reference external worker として提供し、ROS2 node process に sherpa-onnx runtime を混入しない。
- `FA-KWS-SPEC-031`: `backend.name` validation は ROS-free helper と C++ gtest で実行時検証し、node private string check だけに閉じない。
- `FA-KWS-SPEC-032`: concrete KWS backend construction は ROS-free factory に閉じ、ROS2 node は `KwsBackend` interface と `KwsBackendSettings` だけを扱う。

## Integration Test

- `test/integration/test_fa_kws_backend_boundary_contract.py`
  - `include/fa_kws/backends` と `src/backends` の全 backend source が ROS2 / `fa_interfaces` / `AudioFrame` / `VadState` を知らないことを確認する。
  - sherpa-onnx C API は ROS2 node package に link せず、external worker command boundary として扱うことを確認する。

## 未検証の native runtime integration

- canonical fixture WAV を `kws_wav_tool` に通し、backend が検出できることを確認する。
- sample rate / channel / bit depth が canonical でない fixture WAV は `kws_wav_tool` が backend 実行前に reject することを確認する。
- `kws_wav_tool` に `--assume-vad-speech` が無い場合は backend 実行前に reject することを確認する。
- `kws_wav_tool` が `--command`, `--workspace_dir`, `--timeout_sec`, `--sample_rate`, `--num_threads`, `--max_active_paths`, `--num_trailing_blanks`, `--keywords_score`, `--threshold` を explicit backend config として要求し、未初期化の `SherpaOnnxKwsBackendConfig` field を残さないことを確認する。

## Launch Test

- `fa_kws.launch.py` は `node_name` と `config_file` launch argument を必須で受け取り、launch file 内に default を持たない。
- `fa_kws.launch.py` は `FindPackageShare` / `get_package_share_directory` で package-local config を推測しない。
- launch file は backend 名、model path、execution provider を埋め込まない。
- missing model path config で起動失敗することを確認する。
- model / tokens / keywords path が directory または読み取り不能 file の場合に起動失敗することを確認する。
- missing execution provider config で起動失敗することを確認する。
- missing worker command / args / health_args / workspace / timeout config で起動失敗することを確認する。
- invalid `vad.probability_gate` config で起動失敗することを確認する。
- `audio.qos.depth` / `vad.qos.depth` / `output.qos.depth` が 0 以下の場合に起動失敗することを確認する。
- stale VAD state で audio frame が backend に渡らないことを確認する。
- invalid `VadState.probability` で audio frame が backend に渡らないことを確認する。
- identity mismatch の `VadState` で audio frame が backend に渡らないことを確認する。
- VAD gate 未満の fresh `VadState` で audio frame が backend に渡らないことを確認する。
- `AudioFrame.source_id != expected_source_id` または `AudioFrame.stream_id != expected_stream_id` の場合に backend へ渡らないことを確認する。

## Colcon Test

- `CMakeLists.txt` で `ament_cmake_pytest` に `test/` を登録し、`colcon test --packages-select fa_kws` で contract test を実行する。
- `fa_kws_node` / `fa_kws_wav_tool` は sherpa-onnx C API に link せず build/install する。
- sherpa-onnx runtime は external worker command 側の責務とし、node package は `FA_KWS_SHERPA_ONNX` / unavailable backend / dummy backend を持たない。
- test 実行時は ambient pytest plugin を無効化し、host/container 側の無関係な pytest plugin によって KWS 契約テストが skip されないことを保証する。
- `fa_kws_vad_gate_test` は sherpa-onnx C API に依存せず、test-only build でも VAD gate の fail-closed 条件を C++ 実行テストとして検証する。
- `fa_kws_backend_config_validation_test` は ROS2 node を起動せず、missing / unknown / supported `backend.name` を C++ 実行テストとして検証する。
- `fa_kws_backend_factory_test` は ROS2 node を起動せず、missing / unknown `backend.name` と concrete backend config delegation を C++ 実行テストとして検証する。

## Fixtures

- wake word sample
- non wake word speech sample
- silence sample

## 仕様とテストケース対応

### 実装済み

- `FA-KWS-TC-001` -> `FA-KWS-SPEC-001`, `FA-KWS-SPEC-005`, `FA-KWS-SPEC-016`, `FA-KWS-SPEC-025`, `FA-KWS-SPEC-029`, `FA-KWS-SPEC-031`, `FA-KWS-SPEC-032`: `test/unit/test_kws_backend_config_contract.py::test_default_config_requires_explicit_backend_and_execution_provider`, `test_node_fails_closed_for_missing_or_unknown_backend_name`, `test_backend_config_has_no_provider_default`, `test_cmake_removes_native_sherpa_link_mode`, `test_cmake_builds_runtime_targets_with_external_worker_backend`, `test/unit/backend_config_validation_contract.cpp`, `test/unit/backend_factory_contract.cpp`
- `FA-KWS-TC-002` -> `FA-KWS-SPEC-002`, `FA-KWS-SPEC-007`, `FA-KWS-SPEC-008`, `FA-KWS-SPEC-019`, `FA-KWS-SPEC-020`, `FA-KWS-SPEC-023`, `FA-KWS-SPEC-027`: `test/unit/audio_utils_contract.cpp`, `test/unit/test_kws_backend_config_contract.py::test_audio_utils_has_executable_contract_test`, `test_kws_node_rejects_non_canonical_audio_frames`
- `FA-KWS-TC-003` -> `FA-KWS-SPEC-003`, `FA-KWS-SPEC-010`, `FA-KWS-SPEC-011`, `FA-KWS-SPEC-013`: `test/unit/vad_gate_contract.cpp`, `test/unit/test_kws_backend_config_contract.py::test_vad_gate_has_executable_contract_test`, `test_backend_keeps_vad_gate_mandatory`
- `FA-KWS-TC-004` -> `FA-KWS-SPEC-004`: `test/unit/test_kws_backend_config_contract.py::test_detection_score_is_owned_by_backend`
- `FA-KWS-TC-005` -> `FA-KWS-SPEC-009`, `FA-KWS-SPEC-012`, `FA-KWS-SPEC-026`: `test/unit/test_kws_backend_config_contract.py::test_wav_tool_requires_explicit_provider`, `test_wav_tool_rejects_non_canonical_wav_without_hidden_conversion`, `test_wav_tool_validates_wav_before_backend_initialization`, `test_wav_tool_initializes_every_backend_config_field_explicitly`
- `FA-KWS-TC-006` -> `FA-KWS-SPEC-014`: `test/launch/test_fa_kws_launch_contract.py`
- `FA-KWS-TC-007` -> `FA-KWS-SPEC-015`, `FA-KWS-SPEC-017`: `test/unit/test_kws_backend_config_contract.py::test_backend_builds_as_shared_runtime_boundary`, `test/integration/test_fa_kws_backend_boundary_contract.py`
- `FA-KWS-TC-008` -> `FA-KWS-SPEC-018`: `test/unit/test_kws_backend_config_contract.py::test_kws_node_rejects_non_canonical_audio_frames`
- `FA-KWS-TC-009` -> `FA-KWS-SPEC-021`, `FA-KWS-SPEC-022`: `test/unit/test_kws_backend_config_contract.py::test_backend_boundary_fails_closed_for_invalid_runtime_state`, `test_backend_validates_model_paths_and_numeric_config_before_c_api`
- `FA-KWS-TC-010` -> `FA-KWS-SPEC-024`: `test/unit/vad_state_identity_contract.cpp`
- `FA-KWS-TC-011` -> `FA-KWS-SPEC-028`, `FA-KWS-SPEC-029`, `FA-KWS-SPEC-030`: `test/launch/test_fa_kws_launch_contract.py::test_default_config_requires_explicit_backend_and_kws_inputs`, `test_launch_and_install_use_only_declared_kws_node_executable`, `test/unit/test_kws_backend_config_contract.py::test_reference_worker_is_external_entrypoint`

### 未実装 / planned

- `FA-KWS-TC-012` -> `FA-KWS-SPEC-004`: real sherpa-onnx wake word positive/negative fixture は sherpa-onnx external worker runtime が利用できる環境で検証する。
- `FA-KWS-TC-013` -> `FA-KWS-SPEC-006`: stale VAD state と audio callback の結合挙動は ROS graph / node integration test で検証する。
