# fa_sed テスト設計

## Unit Test

- `FA-SED-SPEC-001`: package 化前でも README、仕様書、アルゴリズム詳細説明書、テスト設計、backend docs が存在する。
- `FA-SED-SPEC-002`: `package.xml` と `CMakeLists.txt` を package 化前に置かない。
- `FA-SED-SPEC-003`: 仕様書が unknown label fallback を禁止する。
- `FA-SED-SPEC-004`: backend docs が ROS2 topic/message dependency を禁止する。

## Integration Test

ROS2 package 化後に、fake SED backend で label set、confidence、time range contract を確認する。

## Launch Test

ROS2 package 化後に、missing params file、missing backend、missing model、missing label set で起動失敗することを確認する。

