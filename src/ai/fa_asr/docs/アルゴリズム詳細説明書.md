# fa_asr アルゴリズム詳細説明書

1. node 起動時に `expected_stream_id` が ROS topic 名と衝突しないことを検証する。
2. `AudioFrame` が data を持ち、configured source / stream identity、mono / `FLOAT32LE` / 32-bit / normalized [-1.0, 1.0] / `target_sample_rate` と一致することを検証する。
3. 不一致の frame は backend に渡さず reject する。
4. `VadState.source_id == expected_source_id` かつ `VadState.stream_id == expected_stream_id` であることを検証する。
5. VAD active 中の samples を buffer に追加する。
6. VAD end または timeout で buffer を finalize する。
7. backend に samples と sample rate を渡し、`backend.result_format` に従う output を transcript segments へ変換する。
8. transcript を `AsrResult` として publish する。
9. `TranscribeAudio` request を受けた場合は、numeric `time_range_spec` を parse し、ASR-ready rolling timeline 上で requested `[start,end)` を完全に覆う最小の sample-boundary `[actual_start,actual_end)` へ量子化して slice する。
10. ASR-ready rolling timeline が空なら `window_not_found`、requested range または quantized selected range が retention window 外なら `range_outside_window`、保持済み frame はあるが gap により selected range を連続 coverage できなければ `range_not_continuous` を返す。
11. timeline failure、unsupported `audio_scope` では backend を呼ばず、空 clip、latest frame、`fa_audio_window` の archive/export window へ fallback しない。
12. backend timeout/error/invalid output/empty transcript の場合は `success=false` と明示 error code を返す。
13. 成功時は transcript segment、explicit `timeline.*` parameter 由来の `AudioWindowRef`、backend model ref、resolved time range を返す。

Python version / venv が異なる ASR engine は node 内 import ではなく external worker / process / container backend として扱います。
Resampling / downmix / bit-depth conversion は `fa_asr` では行いません。必要な変換は upstream の format/processing node で明示します。
`source_id` は `expected_source_id` と一致し、`stream_id` は `expected_stream_id` と一致する必要があります。別 source / stream の frame を同じ ASR buffer に混在させません。
VAD state も同じ identity と照合し、別 stream の VAD end で現在の ASR buffer を finalize しません。

## streaming backend の共通アルゴリズム

streaming ASR backend は、non-streaming command backend とは別の状態機械として扱います。
`fa_asr` は VAD / TurnContext / trigger contract によって ASR window を開閉し、backend はその window に対応する stream session を持ちます。

処理順序は次です。

1. node 起動時、backend の `health` を実行して model / worker capability を検証する。
2. `TurnContext.active=true` かつ非空 `session_id` / `user_turn_id` を受け取るまで、ASR result を commit しない。
3. control input が `open_on` 条件を満たしたとき、ASR control window を開く。
4. control window open と同時に backend stream を `start` する。
5. window 中に受け取った ASR-ready `AudioFrame` を sample boundary のまま backend stream へ `audio` として push する。
6. backend から partial が返れば `AsrResult.STATUS_PARTIAL` と `AsrEvent.EVENT_STREAM_PARTIAL_RESULT` を publish する。
7. control input が `close_on` 条件を満たしたとき、window を閉じる。
8. `submit_on_close=true` の場合は backend stream を `finish` し、final transcript を `AsrResult.STATUS_FINAL` として publish する。
9. backend が timeout/error を返した場合は `AsrResult.STATUS_ERROR` と `AsrEvent.EVENT_FAIL_CLOSED` を publish し、別 backend へ切り替えない。

streaming backend の `audio_accepted` は transcript 成功ではありません。
これは「worker が audio chunk を stream state に受理した」ことだけを意味します。
partial / final transcript は別 event として扱います。

## `nemo_rnnt_streaming` backend のアルゴリズム契約

`nemo_rnnt_streaming` は、NeMo RNNT / Transducer model を local `.nemo` file として読み込む streaming 専用 backend です。
`fa_asr` node 本体は NeMo / PyTorch / Parakeet を import せず、model-specific 処理は `nemo_rnnt_streaming_worker` process に閉じ込めます。

`nemo_rnnt_streaming` は JSON Lines protocol で次の command を受けます。

- `health`: `.nemo` restore と capability 検査。
- `start`: stream-local state 初期化。
- `audio`: validated float32 chunk の受理と cache-aware streaming step。
- `drain`: 現時点の partial hypothesis 取得。
- `finish`: stream finalization と final transcript 生成。
- `cancel`: active stream 破棄。

入力 audio contract は次です。

- mono
- `FLOAT32LE`
- normalized `[-1.0, 1.0]`
- finite samples only
- `backend.sample_rate_hz` と一致
- `AudioFrame` timeline 上で連続
- `expected_source_id` / `expected_stream_id` と一致

`nemo_rnnt_streaming` は resample、downmix、PCM16 conversion、WAV decode、value normalization、missing chunk 補完を行いません。
これらが必要な場合は、upstream processing node の責務として明示します。

### NeMo cache-aware streaming の初期化

Parakeet RNNT の cache-aware streaming は、restore 直後の model に raw audio を渡すだけでは成立しません。
worker は model restore 後に、少なくとも次を明示的に検査または設定します。

1. model class が RNNT / Transducer 系であること。
2. encoder が cache-aware streaming API を持つこと。
3. model sample rate が backend config と一致すること。
4. finite attention context が成立していること。
5. decoder strategy が streaming RNNT 用に設定されていること。
6. preprocessor `window_stride` と encoder `subsampling_factor` を取得できること。
7. model chunk size / shift size / pre-encode cache / drop-extra-pre-encoded が整合していること。
8. cache 長込みの maximum audio / positional encoding length を encoder に設定できること。
9. encoder cache initial state を取得できること。

実機で確認した Parakeet `.nemo` は、restore 直後の `att_context_size` が `[-1, -1]` でした。
これは full context を意味するため、そのまま streaming step に流すことは backend contract として認めません。
finite attention context を確立できない場合は、起動時または `health` 時に fail closed します。

また、実機で確認した restore 直後の `streaming_cfg` は、`last_channel_cache_size=10000` を含む full-context 相当の値でした。
NeMo の streaming service 例では、attention context と chunk duration をもとに `encoder.setup_streaming_params(...)` を呼び直します。
FluentAudio 側でも、`backend.chunk_size_samples` だけに依存せず、`window_stride`、`subsampling_factor`、runtime chunk duration、model chunk size から effective streaming params を決める必要があります。

追加調査では、現行 worker と同じ NeMo API を直接呼び、`encoder.set_max_audio_length(last_channel_cache_size + chunk_feature_len)` 相当を streaming step 直前に呼んだ場合、少なくとも最初の `conformer_stream_step(...)` は tensor shape mismatch なしに完了しました。
したがって、`nemo_rnnt_streaming_worker` は `CacheAwareConformerStreamingRunner` 内で、step ごとに cache 長込みの positional encoding length を明示的に成立させる必要があります。
encoder が `set_max_audio_length(...)` 相当を提供しない場合は、empty transcript にせず fail closed します。

### NIM / Riva / NeMo local backend を分けて考える

`nemo_rnnt_streaming` で Parakeet RNNT multilingual を扱うときは、NIM、Riva、NeMo local backend を同じものとして扱いません。

1. NIM は NVIDIA の self-contained ASR serving container であり、model download、optimization、serving API、runtime profile を container 内に持つ。
2. Riva は NVIDIA の speech serving / deployment stack であり、NGC 上の Riva artifact や RMIR と結びつく。
3. FluentAudio の `nemo_rnnt_streaming` は NIM server でも Riva server でもなく、local `.nemo` file を `nemo_rnnt_streaming_worker` process 内で restore する backend である。

公式資料上、Parakeet RNNT Multilingual は streaming と multilingual を support する model family として扱われています。
しかし、その事実は FluentAudio local worker が streaming step を正しく実行できることの証明ではありません。
Riva/NIM は serving stack 内部で model setup、profile selection、decoder configuration、cache 管理を持ちます。
FluentAudio はそれを使わないため、local worker が NeMo の cache-aware streaming contract を一つずつ満たす必要があります。

### local `.nemo` config から読み取る実行前提

現在検証対象の artifact は `nvidia/riva/parakeet-rnnt-riva-1-1b-unified-ml-cs-universal:trainable_v1.0` 由来の `.nemo` です。
この checkpoint は git 管理対象ではなく、NGC CLI などの prepare 手順で local volume に配置します。

取得済み `.nemo` の `model_config.yaml` から、backend が直接使う前提は次です。

| config | 確認値 | algorithm 上の扱い |
| --- | --- | --- |
| `sample_rate` | `16000` | `fa_asr` は 16 kHz mono `FLOAT32LE` 以外を backend に渡さない。 |
| `preprocessor.window_stride` | `0.01` | feature frame は 10 ms stride。chunk duration と encoder token duration の計算に使う。 |
| `encoder.subsampling_factor` | `8` | encoder output token は feature frame の 8 倍の時間幅を持つ。 |
| `encoder.att_context_size` | `[-1, -1]` | restore 直後は full context。streaming backend としては finite context を明示設定できるか検査する。 |
| `encoder.pos_emb_max_len` | `5000` | cache を含む streaming step では不足し得るため、step 前に required length を再計算する。 |
| `decoding.strategy` | `greedy_batch` | RNNT streaming partial/final の解釈と一致するか worker 側で扱う。 |
| vocabulary | `<ja-JP>` を含む | 日本語 model として使う前提に矛盾しないが、transcript 成功の証明ではない。 |

ここで `att_context_size=[-1,-1]` を silent fallback で受け入れてはいけません。
full-context restore state のまま streaming 成功扱いすると、latency と cache の意味が壊れます。
backend は finite attention context を設定できるかを明示的に調べ、設定できない場合は起動時または health 時に fail closed します。

### NeMo 2.7.3 source に基づく streaming step contract

local runtime の NeMo 2.7.3 では、cache-aware streaming は次の API 群の contract で成立します。

1. `ConformerEncoder.setup_streaming_params(...)`
   - `CacheAwareStreamingConfig` を作る。
   - `chunk_size`、`shift_size`、`cache_drop_size`、`last_channel_cache_size`、`valid_out_len`、`pre_encode_cache_size`、`drop_extra_pre_encoded` を決める。
   - FluentAudio worker はこの effective config を backend 内部状態として保持し、health / trace に出せるようにする。
2. `ConformerEncoder.get_initial_cache_state(...)`
   - `streaming_cfg.last_channel_cache_size` を前提に encoder cache tensor を作る。
   - `start` ごとに初期化し、session を跨いで使い回さない。
3. `CacheAwareStreamingAudioBuffer`
   - raw audio または processed signal を streaming step 用 chunk に分割する。
   - pre-encode cache を chunk 先頭へ付与し、`chunk_lengths` を返す。
   - `audio` command の sample 数と NeMo step の feature length は同じ概念ではないため、worker 内で明示的に橋渡しする。
4. `ConformerEncoder.set_max_audio_length(...)`
   - encoder の `max_audio_length` を更新し、positional encoding を拡張する。
   - cache-aware attention は current chunk だけでなく過去 cache を見るため、`last_channel_cache_size + chunk_feature_len` 相当の長さが必要になる。
5. `conformer_stream_step(...)`
   - RNNT/CTC かつ `StreamingEncoder` を前提に `encoder.cache_aware_stream_step(...)` を呼ぶ。
   - `previous_hypotheses`、`previous_pred_out`、`cache_last_channel`、`cache_last_time`、`cache_last_channel_len` を受け取り、次 step 用の状態を返す。

2026-05-22 の失敗では、`health` と `start` は成立した一方、最初の `audio` command で tensor shape mismatch が発生しました。
direct investigation では、step 直前に `encoder.set_max_audio_length(last_channel_cache_size + chunk_feature_len)` 相当を呼ぶと、少なくとも最初の `conformer_stream_step(...)` は通りました。
したがってこの処理は optional optimization ではなく、backend algorithm の必須契約です。

### `nemo_rnnt_streaming` の phase 別アルゴリズム

worker は次の phase を順に成立させます。

1. `health`
   - `.nemo` を restore する。
   - model class、sample rate、RNNT/Transducer、StreamingEncoder、cache-aware API、language token、partial support を検査する。
   - effective streaming config を確立できることを確認する。
   - ここで成功しても、実 audio chunk の streaming ASR 成功とは扱わない。
2. `start`
   - health と同じ config であることを確認する。
   - session-local audio buffer、encoder cache、previous hypotheses、previous decoder output、accepted sample count を初期化する。
   - 既存 session が active の場合は新規 start を拒否する。
3. `audio`
   - `session_id`、wire encoding、byte length、sample count、finite/normalized range を検証する。
   - ASR-ready audio 以外を resample / downmix / normalize して継続しない。
   - buffer に samples を追加し、推論可能な chunk ができた場合だけ cache-aware streaming step を実行する。
   - step 直前に cache 込み required length を計算し、`set_max_audio_length(...)` で positional encoding length を成立させる。
   - `conformer_stream_step(...)` の戻り値から cache / hypothesis を更新する。
   - non-empty text がある場合だけ partial を返す。
4. `drain`
   - 新しい audio を追加せず、現在の partial 状態だけを返す。
   - empty partial を transcript 成功として扱わない。
5. `finish`
   - accepted audio が 1 sample 以上あることを確認する。
   - 残りの推論可能 chunk を処理する。
   - final transcript を返す。
   - final が空で、直前 non-empty partial を final commit に使う場合は、その条件を backend policy として明示する。
6. `cancel`
   - active session の buffer、cache、hypothesis、accepted sample count を破棄する。
   - cancel 後の `audio` / `finish` は拒否する。

どの phase でも、unsupported state を default 値や空 transcript によって隠しません。
worker が説明できる error を返せる場合は JSONL error とし、stdout が閉じた場合は `fa_asr` が stderr を含めて fail closed します。

### 推論 step

`audio` command では次を行います。

1. JSONL payload の `session_id` が active stream と一致することを確認する。
2. `base64_float32le` payload を decode する。
3. sample count と byte length の一致を確認する。
4. sample が finite かつ normalized range 内であることを確認する。
5. worker-local buffer に追加する。
6. 推論可能な feature / audio chunk ができた場合だけ cache-aware streaming step を実行する。
7. step 直前に cache 長込みの maximum audio / positional encoding length を encoder に設定する。
8. RNNT previous hypothesis と encoder cache を更新する。
9. non-empty partial が得られた場合だけ partial result を返す。
10. chunk を受理したことを `audio_accepted` として返す。

worker は tensor shape mismatch を握りつぶして空 transcript にしてはいけません。
NeMo 内部で shape mismatch、unsupported attention context、unsupported streaming config が発生した場合は worker error として stdout を閉じるのではなく、可能な限り JSONL error として返し、`fa_asr` は fail closed します。

### 実機検証で確認した未成立点

2026-05-22 の full ROS graph 検証では、`fa_vad` が `start=true` を出し、`fa_asr` が `EVENT_CONTROL_WINDOW_OPENED` と `EVENT_STREAM_OPENED` まで到達しました。
しかし最初の audio push で `nemo_rnnt_streaming_worker` が終了し、`AsrResult` は `STATUS_ERROR` / `stream_audio_push_failed` / empty text になりました。

worker stderr の本質的な error は次でした。

```text
RuntimeError: The size of tensor a (10001) must match the size of tensor b (5001) at non-singleton dimension 3
```

これは ROS topic、VAD identity、TurnContext の問題ではなく、worker 内部の NeMo cache-aware streaming step contract が未成立であることを示します。
追加調査では、`pad_and_drop_preencoded` の切り替えだけでは同系統の mismatch は解消せず、`encoder.set_max_audio_length(...)` 相当を step 直前に呼ぶことで最初の streaming step が通ることを確認しました。
したがって、この backend は health check 成功だけで完了扱いしてはいけません。
少なくとも worker 単体で `health -> start -> audio -> audio -> finish` が成功し、その後 full ROS graph で non-empty final transcript を確認する必要があります。

command 系 backend は起動時に command/model path と `backend.args` placeholder を検証します。`backend.result_format` は `plain_text` または `segments_json_v1` として明示必須であり、default config は空のままにします。model-id worker backend はさらに `backend.health_args` を必須にし、`{model}` を含む startup health check を通過しない限り backend を起動完了扱いしません。実行時には canonical `AsrRequest` を `float32le_raw` payload として一時 `.f32` に serialize し、external worker に `{audio}`、`{model}`、`{sample_rate}` を渡します。`AsrRequest` が float32 1D、非空、正の sample rate、finite/normalized samples を満たさない場合は worker を起動しません。bit depth / value range 変換はここで行いません。
`plain_text` は transcript text を selected ASR request samples 全体の 1 segment にします。`segments_json_v1` は top-level が `result_format` と `segments` だけの strict JSON で、各 segment の `start_sample` / `end_sample` は selected ASR request samples からの相対 sample index です。invalid JSON/schema/range、overlap、空 text、空 `speaker_label` は fail closed であり、`fa_asr` は推測補正、型 coercion、resample、downmix、PCM/int16 変換を行いません。
`TranscribeAudio` service も同じ ASR-ready timeline と backend contract を使います。request start/end が sample boundary と一致しない場合は、`floor(delta_ns * sample_rate / 1e9)` / `ceil(delta_ns * sample_rate / 1e9)` で selected sample index を決定します。response の整数 ns range は selected start index を `floor(index * 1e9 / sample_rate)`、selected end index を `ceil(index * 1e9 / sample_rate)` で frame start に加算して表現し、selected samples を覆う範囲として返します。整数 ns で正確に表現できない sample 境界では、前 frame の exact end boundary を整数化した `[floor,ceil]` envelope 内の next start を contiguous として append します。next start が floor より前なら overlap として reject し、ceil より後なら append は許可しますが、slice がその区間を跨ぐと `range_not_continuous` を返します。backend には selected range の float32 samples だけを渡します。backend が返す transcript segment start/end は selected range からの相対 sample index であり、response では selected start から `floor(start_sample * 1e9 / sample_rate)` / `ceil(end_sample * 1e9 / sample_rate)` で absolute unix ns に変換します。response の `time_range` と `audio_window_ref.time_range` も selected range を返します。`fa_audio_window` が提供する PCM16 archive/export window は証拠 clip 用であり、`fa_asr` はそこから WAV decode や PCM16 -> float32 conversion を行いません。両者を同じ time range で参照する場合は、system profile で同じ upstream stream から分岐し、`AudioFrame.header.stamp` を保存することで time alignment を成立させます。
`window_not_found` は ASR-ready timeline が空で requested range を判定できない状態、`range_not_continuous` は保持済み frame はあるが requested range 内に gap があり連続 coverage できない状態です。後者は「未蓄積」ではなく coverage failure なので、backend に短い audio や空 audio を渡さず、明示 error として返します。
