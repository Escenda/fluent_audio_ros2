# Smart Turn v3 model

Place `smart-turn-v3.0.onnx` in this directory, or set `model_path` in `config/default.yaml`.

The node refuses to start when the configured model is missing or when the file is a Git LFS pointer instead of the actual ONNX payload.
