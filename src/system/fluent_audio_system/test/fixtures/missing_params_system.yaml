system:
  default_start_delay: 0.0
  inter_group_delay: 0.0
groups:
  - id: io
    enable: true
    nodes:
      - id: fa_in
        enable: true
        package: fa_in
        exec: fa_in_node
        params_file: "${share:fluent_audio_system}/test/fixtures/not_found.params.yaml"
